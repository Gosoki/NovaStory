# 03 · 测量与数据字典

> 来源合并：旧 paper/14 §1-2·§5（维度背书 / 量表来源 / 客观评价栈）+ paper/10 §6（测量表）+ paper/12（数据字典与可计算指标）。
> 以代码为准：`core/db.py`(schema)、`core/state.py`(log_event/round_durations)、`views/_trial.py`(落库)、`views/_streaming.py`(LLM 事件)。

---

## 1. E 三固定维度的内容效度背书

**结论：三维（psychology / turning_point / key_shot）都有真实、可查的权威理论背书，足以回应审稿人「凭什么是这三个」。**

| 维度 | 背书文献（核实级） | 支持点 | 已应用的措辞校准 |
|---|---|---|---|
| **psychology** | McKee《Story》(1997)：“desire is the key to character / what do I want”[B]；Bordwell & Thompson《Film Art》：goal-oriented protagonist、人物动机=因果链引擎[概念 CONFIRMED] | 人物欲望/动机是人物成立与叙事驱动的根 | 从「内心感受（心情）」**收敛到「欲望/动机」**——问「最想得到/最想避免什么」，不停在情绪形容词 |
| **turning_point** | Syd Field《Screenplay》：plot point = 把动作推向新方向的转折事件[概念 CONFIRMED]；McKee：每个 scene 靠「价值正负翻转」turn[A 逐字] | 转折是结构骨架，且可下沉到 15 秒微型脚本 | 把「抓住观众（hook = 效果）」与「转折（= 要素）」**分开**；用「价值/情势翻转的一瞬」锚定 |
| **key_shot** | Katz《Film Directing: Shot by Shot》：visualization 起点是 single arresting image，用来找 dramatic center[A 逐字]；日本絵コンテ实务：構図是标准记载项[B] | 关键画面撑起戏剧核心，不只是「好记」 | 从「最想让人记住」**改到「用哪一格·什么构图撑起戏剧核心」**，顺带要求构图 |

**论文内容效度段写法**：三维 = 从 Katz（视觉）+ Field / McKee（叙事）三支柱各取「新手最缺的一个必答项」，非随意挑选。

### ⚠️ 引用逐字性修复清单（投稿前必改）

对抗核查 6 条中 4 条完全 CONFIRMED，2 条 DOUBTFUL **仅在逐字性、非存在性，无捏造**：

1. Bordwell 那句「Characters and their motivations are the main driving force…」**全网零逐字命中，是转述冒充直引** → 论文改为转述，或找到确切页码原文；
2. Syd Field 的引用被改写了，**真实原话是**「any incident, episode, or event that hooks into the action and spins it around **in** another direction」；且「reversal」是 McKee 用语不是 Field 的 → 用 Field 真实措辞；
3. 絵コンテ「必須」应软化为「**標準/主要項目**」（来源没明说 required）。

### 已撤销：pacing（尺配分）第四维 — 留档

案头研究曾识别出「时间/节奏·尺配分」是比现有任一维更贴合 15 秒/3 镜设定的漏项（絵コンテ把「秒数・尺」列为标准必填），并一度采为「高优先补充维」。
**2026-07-18 用户拍板撤销**：生成 prompt 本就让 AI 自行分配每镜时长，再回头问新手「15 秒怎么切给三镜」是**冗余且新手答不出**的问题，只会膨胀 E 的提问数、拖累前期耗时。三支柱不变；补充维改由 AI 按创意自由补（tone/ending/sound/detail）。
**并显式禁止 AI 就时长/秒数提问**（round1 + followup 两处 prompt，三语齐）。案头分析记录保留于此，不进实现。

## 2. 量表来源与效度

### 2.1 主导感 soa1-2 —— J-SoAS SoPA 情境化改编 ✅

- **来源（核实级 [A]，全 9 项逐字核对 PMC Table 1）**：Xu, W., Legaspi, R., Ishikawa, Y., & Washida, Y. (2024). *Measuring general sense of agency: a Japanese adaptation and validation of the sense of agency scale (J-SoAS).* Frontiers in Psychology, 15, 1427169. —— 对 Tapal et al.(2017) SoAS 的**日语验证版**（N=8,237，SoPA 正向 / SoNA 负向双因子）。
- **映射**：soa1（主导权）← SoPA Item4「私の行動を決めているのは私だ」；soa2（掌控感）← SoPA Item1「私は自分のやることを完全にコントロールできている」。
- **已应用（ja/zh/en 三语同步）**：
  - soa1 ja「この絵コンテの内容や方向性を決めていたのは、私だ。」
  - soa2 ja「この絵コンテ制作の過程を、私は自分でコントロールできていた。」
- **关键取舍（必须诚实标注）**：J-SoAS 原句测的是**一般能动感（my actions）**，我们**情境化到本次创作**（否则构念与因变量错配）。→ 论文写「改编自已日语验证的 J-SoAS **SoPA 子量表**并情境化到本任务」，**只用 SoPA 子集、须基于所用子集重算信度**，不能直接搬原文 α。
- soa1 显式定位为**操纵/收敛检查**，soa2 留作 agency 指标（预注册时锁）。

### 2.2 心理所有权 own1-3 —— 维持，两处诚实处理

- **来源**：Van Dyne & Pierce (2004), *J. Organizational Behavior* 25(4):439-459 [CONFIRMED]。own1-3 是**自译改编**、target 从「组织」换成「作品」。
- ① **不能宣称「验证过的日语量表」**，写「参考 Van Dyne & Pierce、自译改编到作品对象，存在翻译/文脈化限界」；
- ② **own3「アイデアやスタイルが反映されている」是 self-investment（自我投入）维度**（Pierce 2001/2003 心理所有权三路径之一），不在纯 possession 7 条内。**决定：保留 own3 不改措辞**，论文标注为「Pierce 自我投入维度」，与 own1/own2（possession）一并构成所有权复合——self-investment 是心理所有权的正当组成、非错项，只需诚实标注，并在报复合信度时说明其含 **possession + self-investment 两个 facet**（预注册时锁定分报规则）。
- 投稿前调 Wiley 原文，把 Van Dyne & Pierce 完整 7 条与 own3 归属逐字定稿。

### 2.3 效度流程（无 pilot 下的调整）

- **回译（Brislin/ITC）**：≥2 名双语者正译 → 调和 → 回译 → 比对语义漂移 → 迭代；J-SoAS 已有官方日语，只对情境化引导语小校核；**own1-3 走完整回译**。
- **认知访谈**：5-8 名同质日本大学生 think-aloud + 探询（paraphrase / 关键词理解 / own1 vs own2 是否冗余 / 锚点够不够）。
- **信度报告**：每个（子）量表分开报 α；2-3 条时补 inter-item r / Spearman-Brown / 复合信度 CR+AVE。
  **无 pilot → α/CR 只能正式数据事后报，Limitations 必写「量表未经独立预试」。**

### 2.4 其他量表出处

| 构念 | 条目 | 出处 |
|---|---|---|
| 负荷 | tlx1 | NASA-TLX 简版（Hart & Staveland 1988；论文注明改编） |
| 意图违背 | violation（反向） | 自编（配合保真三角） |
| 想象匹配 | imagine_match | 自编（不要求语言化的保真主观侧） |
| 本轮满意度 | satisfaction（每轮 1 题） | 自编（结果测量；与所有权分离检验用，H7） |
| 逐镜头归属 | mine / ai_ok / ai_against 比例 | 自编（保真的细粒度） |
| 整体偏好 | final_survey（最喜欢哪轮/以后用哪轮/总体满意度/留言） | 自编（三轮后一次性，轮次→条件由 trials 映射） |

**量表点数**：全部 **7 点**（源量表原生 7 点，2026-06-25 拍板保留）。
**⛔ 不再讨论**：加反向/额外问卷题来控应答风格 —— 2026-07-19 决定**不加**（被试内差分已扛住主威胁；为边际收益加被试可见题、还冒反向题测量风险，对 N=36 不划算）。敷衍剔除已由 `attention_raw` + 块内 `straightline` 直线作答检测 + 可事后算完成时间覆盖。Limitations 写「应答风格主要靠被试内差分控制」。

## 3. 保真三角（⚠️ 现行口径）

> **保真三角 = 想象匹配（自评，主观）+ 逐镜头标注（细粒度）+ 埋め込み距離 Δ（客观、自动）**
> **三者收敛才下强结论。**
> ⚠️ 旧文档写的「第三腿 = 盲评保真（外部）」**已作废**——LLM-judge 降为**收敛证据 / 次要**，不进主复合。

**为什么被试自评是主保真锚**：意图保真的正确裁判是**被试本人**——只有被试知道心中的像，外部人只看得到 1-2 句创意。
**Limitations 诚实写「无外部真人保真锚」**；答辩答法即上句 + 引 TTCW 说明为何不用 LLM 评创意。

## 4. 客观评价栈（数据科学式，全部相对 baseline_gen 表述增量）

| 目标 | 主力 | 补充 |
|---|---|---|
| **保真** | 多模型 embedding 相似度的**相对基线增量 Δ** = sim(创意,终稿) − sim(创意,baseline_gen)，≥3 个支持日语的异构模型取均值 + 一致性带 | 逐镜头作者标记 ai_ok/against 占比；终稿↔首版 AI 稿编辑/embedding 距离；chrF 仅当「关键词落地率」降权 |
| **质量** | **结构完整度**（parse_ok + 分镜四字段齐全率 + 15s/3镜达标率）= **客观下界**，直接呼应灵魂句 | 词汇丰富度 MATTR/HD-D + 日语可读性，**仅描述剖面、不作审美主张** |
| **多样性** | gzip 压缩比 CR + self-repetition + self-BLEU（Shaib 2024 组合，同题分组比 C/D/E） | 相对 baseline_gen 的语义同质化 z 分 |
| **可靠性** | 多 embedding 模型 + 多指标之间 Spearman / ICC / Krippendorff α（无真人评审下的信度替身） | — |
| **机制** | 努力再分配（E 的 t_pregen vs D 的 t_postgen/返工量）+ E 引导剂量对保真的剂量-反应回归 | 版本演化曲线 |

**为什么不靠 LLM-judge 评质量**：TTCW（CHI 2024）证明 GPT-4/Claude 与专家的创意评测**相关接近零、无一正相关**；LLM-judge 还有位置/长度/自我偏好偏差且不可复现。→ **「质量/创意好坏」不交给 LLM-judge 当客观分。**

**诚实下界（已拍板）**：**「创意质量」本质上无法纯客观测**（TTCW 实证下界）。锁定路线 = ① 结构性代理（客观下界，明说）+ ② **弱主张 = 不劣性检验（TOST）**——不主张「E 更好」，改主张「E 在保真/多样性上不劣于 D，且事后返工更少、事前投入更多」。
~~③ 极小样本人评效度锚~~ → **不做**（不引入招募人评的不稳定因素，2026-07-18 再确认）。

**LLM-judge 的残余定位**：OpenAI 系 ×3 取均值 + 报自一致性作可靠性代理；**固定次要/探索，不进主复合**；引 Zheng 2023（结构化评分可用）+ TTCW（创意维不可靠 → 只评保真不评创意）界定适用范围；Limitations 明示「单家族 + 无人评」。
⚠️ `analysis/judge.py` 现状与此口径有差距（缺盲评保真模式、质量四维与 rubric 不一致、提示是中文而评审对象是日语、docstring 仍写已作废的「异家族」协议、`--human-sample` 已废弃），脚本挂着过时横幅 → 是否重写见 [06 #5](06_待拍板事项.md)。

## 5. 论文测量表（表1 的展开）

### 5.1 客观测量（行为日志与产物，全自动）

| 构念 | 指标 | 来源 |
|---|---|---|
| 事前投入 | `t_pregen`、第1轮自填率、AI 代答率、自填字数 | guidance_json / events |
| 事后修改 | `n_ai_rounds`、`n_hand_edits`、`hand_edit_chars`、`t_postgen` | trials / events |
| 总投入 | 上两者之和 + `t_total`（**三口径并报**，防「把工作量挪到前面」质疑） | 同上 |
| 意图保真（客观侧） | 创意↔终稿 embedding **相对基线 Δ**（保真主复合候选成分，pilot 收敛验收建议 r ≥ .3，不达标降次要） | `analysis/embed.py` |
| 版本演化 | 逐版 diff 轨迹、AI/人改比例 | script_versions |
| （探索）同质化 | 条件×题目内两两相似度 vs baseline_gen 机器地板 | `scripts/baseline_gen.py` |

> ⚠️ **招牌图注记**：C/D 的事前投入按构造记 0 —— 图上必须加注，或把 `t_read_intent` 折进 total_investment，**别被读成「新手事前零思考」**。

### 5.2 主观测量（应用内即时量表，1-7）

见 §2.4 + 心理所有权 own1-3（α 预期 >.8）+ 状态 agency soa1-2。

## 6. 数据落点（SQLite 四表）

| 表 | 要点 |
|---|---|
| `participants` | `demographics_json` / `screening_json`（含 is_novice 五项、aiexp/trust/own_trait 基线协变量、**dev 标记**）、`attention_raw`、`token`（续接句柄）、`final_survey_json`、status（in_progress / screened_out / done）、拉丁方 `seq`、完成码 |
| `trials` | `guidance_json`（E 逐轮问答全记录）、`revision_requests`（D 反馈原文）、`script_versions`（每版全文 + 作者 ai/user_edit）、`n_ai_rounds`、`n_hand_edits`、`hand_edit_chars`、`t_read_intent`/`t_pregen`/`t_postgen`/`t_llm_wait`/`t_total`、`parse_ok`、模型参数（model/temperature/base_url 逐 trial）；唯一索引 `(participant_id, round_idx)` + INSERT OR REPLACE |
| `events` | 毫秒级交互日志；每行带 `seq_in_round`（轮内序号）/ `attempt`（会话段 id，重做轮切段）/ `trial_id`（提交后回填中签段）；`llm_done` 携带 token usage |
| `questionnaires` | own1-3 / soa1-2 / tlx1 / violation / imagine_match / satisfaction / 逐镜头标注 / **E-only：ai_q_quality、ai_q_amount、ai_q_best_json**；唯一索引同 trials |

### 6.1 事件流时间线（一轮）

```
round_start
→ intent_submit {chars}
→ [E] guidance_shown {round, source, n_questions, fallback}
→ [E] guidance_answer_saved {round, q} ×每次翻页（相邻两条的毫秒差 ≈ 每问耗时）
→ llm_start {group} → llm_done {group, elapsed, usage:{prompt,completion,total_tokens}} / llm_error {group, elapsed[, kind]}
→ [E] guidance_answer ×每问 + guidance_submit {round}（两者都在终稿/修改稿生成**成功后补记**，落库顺序在 llm_done 之后）
→ script_shown {v}
→ hand_edit_saved {v, chars_delta} / revision_request {chars} / continue_guidance_click
→ （循环…）
→ trial_submit（→ 回填 trial_id：本 attempt **截至此刻**的事件；其后的 questionnaire_submit、最后一轮的 final_survey_submit 仍为 NULL）
→ questionnaire_submit
```

另有：`session_resumed`（刷新续接）、`final_survey_submit`、`dev_switch_condition`（研究员）。
`group` 取值：`C-final` / `D-final` / `D-revise` / `E-guidance-r1` / `E-guidance-fu` / `E-final` / `E-revise`。

**重做轮切段**：同 (participant, round) 下按 `attempt` 分组；进了论文的那次 = 组内含 `trial_id` 非空行的组。
⚠️ **不要用 `WHERE trial_id IS NOT NULL` 过滤事件重建时间线**（会丢 questionnaire_submit 等提交后事件）——先按 trial_id 定位中签 attempt，再按 `(participant_id, round_idx, attempt)` 取全组。

## 7. 完整可计算清单

### 被试级
人口学/筛查逐题（**index 存储、语言无关**）、is_novice 及其五个操作化子项、基线协变量、语言、Williams `seq`、状态漏斗（screened_out / in_progress / done）、完成码、`attention_ok` + `attention_raw`、全程总时长（`participants.created_at` → `final_survey_submit`）、全程 token 成本（Σ `llm_done.usage.total_tokens`）。

### 轮级
- 条件 / 题目全文 / 创意 / 终稿 / `parse_ok` / 模型参数
- **时间五列**（LLM 等待均已剔除或单列）：`t_read_intent`（读题+写创意）、`t_pregen`（仅 E，第 1 轮引导答题净时，扣窗内生成等待）、`t_postgen`（首版稿→提交，已扣 AI 等待）、`t_llm_wait`（本轮全部 AI 等待，含失败重试）、`t_total`（round_start→trial_submit，**不含问卷**；问卷时长须单独从 events 算）
  > ⚠️ 命名注记：`r_llm_wait_pre` / `r_llm_wait_post` 命名暗示互补，实为**非对称语义**，三者不是一个划分。
- **行为计数**：`n_ai_rounds`（D 修订轮 / E 追问轮）、`n_hand_edits`、`hand_edit_chars`
  > ⚠️ **轮数口径差异声明**：E 一轮 ≠ D 一轮，论文必须写明
- **E 引导剂量**（guidance_json）：每问 `is_custom`（自填率）/ `ai_decided`（AI 代答率）/ 所选内容；问题数；`fallback`；追问轮数及每轮基于哪版稿（`draft_snapshot_ref`）
- **D 反馈素材**：每条原文 + 顺序 → 内容编码、图4
- **版本演化**：script_versions 全链 → 任意两版 diff、AI/人改比例、逐版长度轨迹、终稿↔首版 AI 稿距离
- **逐镜头**：`parse_shots(终稿)` × `shot_annotations_json`（mine / ai_ok / ai_against per shot）
- **每轮问卷**：全部 1-7 量表 + E-only 三题

### 四个关键问题的可计算性（已验证）
1. **每次提交了什么（内容级）** ✅ 完成的轮全文可得（创意 / 每版 AI 稿 / 每版手改稿 / 终稿 / D 每次反馈 / E 每问答案 / 三份问卷全文）
2. **点击生成 → AI 生成完的时间** ✅ 精确可得（`llm_done.payload.elapsed`，浮点秒；失败调用同样有；轮级合计 `t_llm_wait`）
   - 口径注意：`elapsed` 含首 token 前自动重试的总耗时 = **用户实际等待**口径；「模型纯生成/首 token 延迟」口径在运维日志 `data/llm.log`
3. **脚本显示 → 用户下次点击的时间** ✅ 毫秒级可得；有无修改三重可判（`n_hand_edits` / `hand_edit_chars` / 逐字 diff）
4. **问卷显示 → 问卷结束的时间** ✅ 毫秒级可得（`trial_submit` → `questionnaire_submit`）

## 8. 算不出的（缺口清单，已接受）

| 缺口 | 处置 | 说明 |
|---|---|---|
| 中途退出轮的内容（创意/答案/版本） | **放弃**（拍板①：不完整数据弃用） | trials 提交时才写；events 无内容 |
| 打字过程的时间分布 | 接受 | 编辑器无键入埋点；只有窗口总时长 + 改动量 |
| consent 页停留时长 | 接受 | 无被试行、无事件 |
| 问卷逐题作答时刻 | 接受 | 整页一次提交 |
| 引导步模型 meta 单独落库 | 已消解 | 引导步与主模型同为 OpenAI；真想留审计痕迹可低优先补记 |

⚠️ **已知口径污染源（分析时注意）**：
- `views/guidance.py` 的 `guidance_answer_saved` 在 **E-final 生成失败重试 / 回看**时会对同一问重复落库 → 逐题耗时分析取 `(attempt, q)` **末条**即可。
- `core/llm.py` `generate_json` 中途 API 报错时，前序 attempt 已耗的 token 从日志丢失（成本口径不含失败调用）。
