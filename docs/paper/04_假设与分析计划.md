# 04 · 假设与分析计划

> 来源合并：旧 paper/10 §5.4·§7（假设表 / 统计分析 / 图表）+ paper/8 A4（主要终点拍板）+ paper/17（方法学修正）+ `analysis/prereg.py`。
> ⚠️ **阈值 / SESOI / 终点层级 / 复合公式的单一真源是 `analysis/prereg.py`**，本文是它的散文版，两者不一致以代码为准。

---

## 1. 假设表（v3，采数前锁定）

| # | 假设 | DV | 方向 | 主对比 / 检验 | 性质 |
|---|---|---|---|---|---|
| **H1** | 意图保真 E > D > C | **保真复合**（想象匹配 + 违背反向 + 逐镜头 mine 率 + embedding Δ） | E 最高 | **E−D（主）** / E−C / D−C，Holm | 确认性 |
| **H2** | 心理所有权 E ≥ D > C | **所有权复合**（own1-3，含 possession + self-investment） | E 不低于 D、均 > C | E−D / D−C | 确认性 |
| **H3a** | 事后返工 E < D | `effort_composite` = mean z(log1p(n_ai_rounds, hand_edit_chars, t_postgen)) | E 更少 | E−D | 确认性 |
| **H3b** | 努力再分配：E 事前投入前移且总投入不劣 | `t_pregen` / 三口径总投入 | E 前移 | 描述 + E−D | 确认性 |
| **H4** | 质量非劣：E 不劣于 D | 结构完整度（客观下界） | \|E−D\| < 事前锁定的 SESOI | **TOST** | 确认性 |
| **H5** | 剂量-反应：E 内事前投入↑ → 保真↑ | 保真 ~ 事前投入 | 正 | **被试间 OLS**（每被试仅 1 个 E 轮） | 探索→**已升为事前声明的确认性次分析**（承接归因） |
| **H6** | 同质化：条件影响产出趋同度 | 组内 CR / 语义 z vs 机器地板 | C 更同质？ | 组级 | **探索** |
| **H7** | 反向分支：满意度与所有权分离（新手或偏好 D） | satisfaction × 所有权 | 解离 | 象限判定 | **反向/悬念**（Knijnenburg 2011 / Slovic 1995） |
| **H8** | 经验调节：E 的收益在**无经验者**身上更大 | 主复合 × `is_novice` / `aiexp_idx` | novice 增益更大 | 条件 × 经验 交互 | **探索**（B1 拍板，2026-08-03） |

> ⚠️ **H3a 与 H3b 不能合并**——旧幻灯片曾把两者压成一个 H3，后已拆回。H3a = 事后 E<D（可能撞地板），H3b = 再分配（E-only，不依赖 D 的返工空间）。

**H5 的规格修正（重要）**：每被试仅 1 个 E 轮 → **无法被试内中心化**，实为**被试间关联（OLS）**，受被试间混淆限制；报 CI、定位为机制探索而非因果。（旧文档写的「被试内中心化」是规格-实现矛盾，已作废。）

**归因链**：H5（剂量-反应）+ **D 反馈内容编码（图4）** 一起承接「为什么 E 有效」的归因，两者均为**事前声明的确认性次分析**（从探索升级），因为主对比 D-vs-E 是工作流包层的多变量对比。

## 2. 主要终点（A4 拍板；试测后、采数前冻结锁定）

**主要终点只有两个**（`prereg.PRIMARY_ENDPOINTS`）：

### 2.1 保真复合 `fidelity_composite`
= 想象匹配 + 意图违背（**反向**）+ 逐镜头 mine 比例 + **创意↔终稿 embedding 相对基线 Δ**
各成分**按全样本 z 标准化**后加权（公式的单一真源 = `analysis/prereg.py` 的 `COMPOSITES`）。

> **z 用全样本、不用被试内**（2026-08-03 拍板）：每被试每条件仅 1 轮，被试内 SD 只由 3 个点估计、噪声过大（散布小的被试会被放大成极端值），且与 LMM 的随机截距 `(1|被试)` 功能重叠——被试间差异交给随机截距吸收，标准化阶段不重复处理。

- **pilot 验收规则**：embedding 与其余三成分方向一致且收敛相关达标（**建议 r ≥ .3**）→ 留；不达标 → **降次要**，复合缩为三件套。
- 这也是 JP2（日语短文本 embedding 可比性存疑）的处置。
- **主观三腿先平均、再与 embed 各半**（2026-08-03 拍板并已实装于 `stats.build_composites`）：
  `0.5 · mean(z(想象匹配), z(−违背), z(mine率)) + 0.5 · z(embedding Δ)`。
  此前代码是四腿等权 → 唯一的客观腿被稀释到 25%、3 条自评腿合占 75%；答辩火力点正是「无外部真人保真锚」，客观腿不能只占四分之一。
  embedding 缺席（未跑 `make embed`，或 pilot 收敛 r<.3 判为降次要）→ 退回主观三腿等权**并告警**；逐行缺 embedding 的行记 NaN 而**不**退回主观均值（否则同一终点里混两种权重，被试间比较有偏）。
- 缺失数据规则在冻结时锁定。
- 逐镜头 `mine_ratio` 当**序数**处理 / 分条件报；C 条件的回溯归因考虑不入池。

### 2.2 所有权复合 `ownership_composite`
= own1-3 均分（**soa1-2 次要，但都保留、都报告**）。

- own3 = self-investment facet，**possession（own1/2）与 self-investment 分报**。
- 若 pilot 显示 own 有测量问题（日语 α 崩 / 天花板），可在**冻结前**改判为 soa 或双主终点——**冻结后、正式数据到手后不可再换**。
  （把「哪个好用用哪个」合法化为 **pilot 决策**而非正式数据决策。）

### 2.3 次要终点 `prereg.SECONDARY_ENDPOINTS`
`satisfaction`、`effort_composite`、`post_investment`、`total_investment`。

**族错误控制**：两个主复合上做 FWER；次要 / 探索**门控其后**（正式族在SAP 锁定）。

## 3. 统计分析（冻结后执行）

### 3.0 主分析人群（B1 拍板，2026-08-03）

> **纳入规则（2026-09-02 补，`prereg.ANALYSIS_REQUIRES_ALL_ROUNDS`）**：先排除**未走完全部
> 3 轮**的被试，再在剩下的人里取 novice 子集。以**问卷提交数**为准而不是
> `participants.status`——三轮问卷都交了、只差最后那份总问卷没点的人，任务数据是完整的。
> 这条以前只写在 `v3.py` 的一句注释里、**从未执行**；现在在 `v3.load()` 里真的跑，
> 并打印排除人数。它同时是同意书「中止した場合、そこまでのデータは分析に使用しません」
> 那一句的实现——**改它等于改一句已经对被试作出的承诺**。

- **招募不设门槛**（随机找人），但**事前确定的主分析人群 = `is_novice` 子集**（5 项严格 AND，`prereg.NOVICE_DEF`）。
- **全样本**分析作为**稳健性**并列报告。
- **有创作经验的被试不浪费**：单列出来做 **H8「経験あり vs なし」对比 / 调节分析**（探索性）——这也顺带回应 APE「经验不调节效应」的对照点。
- ⚠️ **两个连带动作**：① 功效必须按 **novice 子集**（更小 N）重算，`power_sim` 加子集功效曲线；② 停止规则改为**超招募到 novice 子集也达标**，不是全样本 36 就停。
- ⚠️ 达标率若 <40%（`pilot_check ③` 🔴）：招募端加软门槛（杠杆最大）或**在冻结时**放宽为 4-of-5 —— 不能看了正式数据再改。

### 3.1 统计流程

1. **主分析**：每个 DV 拟合 LMM
   ```
   DV ~ 条件 + 题目 + 顺序位置 + (1 | 被试)
   ```
   计划对比 **E−D（主）**、E−C、D−C，**族内 Holm 校正**。
2. **稳健性**：Wilcoxon 符号秩（配对）；Likert 单题用 **CLMM**（序数）复核；计数类（轮数 / 手改次数）用**负二项**混合模型；时长 log 变换。
   - 单终点 `n_ai_rounds` 的确证检验用负二项 → SAP 锁。
3. **非劣（H4）**：**TOST**，等价界用**事前锁定的绝对 SESOI**（⚠️ 不可用数据依赖的 `bound_sd × sd`——已修正）。
4. **剂量-反应（H5，E 内）**：保真/所有权 ~ 事前投入（自填率、答题净时、1−AI 代答率）；**被试间 OLS**，报 CI。
5. **敏感性**：
   - 加总时长协变量（正面处理时间混淆）
   - 仅第 1 轮的**组间**分析（防残留）
   - 剔除非 novice / attention 失败 / straightline / fallback 轮
   - 更换 embedding 模型
   - intent 长度 / 维度覆盖特征作协变量（全事后可算，接 `pilot_check`）
6. **探索**：同质化（置换检验 + 机器地板）、D 反馈编码统计、中介（事前投入 → 违背↓ → 所有权↑，**仅探索**）。
7. **功效**：`analysis/power_sim.py`——**与主分析同构的 LMM + Holm 功效**（不是配对 t）；已报 N=36 → 80% 功效 dz ≈ 0.48。
   冻结前跑 ρ ∈ {.3, .5, .7} 敏感性，**报功效曲线而非单点**。
   ⚠️ **主分析人群 = novice 子集（B1）→ 必须另算子集功效曲线**（N 更小 → 可检出的 dz 更大），并据此定超招募目标。

### 3.2 数据清洗硬规则

- **dev 被试必须排除**：`v3.load` + `pilot_check.check_novice` 按 `screening_json.dev` 排除（曾实测 6 trials 中 4 条是 dev 走查行）。
- 不完整数据（未走到 trial_submit / final_survey 的 in_progress 被试）**弃用不进任何分析**。
- 重做轮取「中签 attempt」（含非空 `trial_id` 的事件组）。

## 4. D 反馈内容编码（图4 的机制链）

把 D 的 `revision_requests` 按「**是否落在 E 的引导维度上**」编码——若 D 用户事后修的**恰是 E 事前问的**，机制闭环。

- **编码者**：研究员单编码 + **LLM 作第二编码者**，报**人-机一致性**，Limitations 说明（原「两名编码者 + 一致性」因无外部人力改此备选）。
- codebook：`revision_requests` → 引导维度（psychology / turning_point / key_shot / tone / ending / sound / detail / other）。⚠️ **codebook 终稿待定**。

## 5. 图表清单

| 编号 | 内容 |
|---|---|
| **图1** | 三工作流泳道图（C/D/E 并排，标注人类输入点） |
| **图2** | 主要 DV 分条件：云雨图/箱线 + **被试内连线**（配对设计的标准画法） |
| **图3（招牌图）** | **「努力再分配」**：每条件的事前/事后投入堆叠条（时间与动作数双版本）——直接可视化灵魂句。⚠️ C/D 事前投入按构造记 0，图上必须加注 |
| **图4** | D 的反馈内容 → 引导维度映射（机制图：「事后修的 = 事前没问的」） |
| **表1** | 测量一览 |
| **表2** | 描述统计 |
| **表3** | LMM 估计与对比 |
| 定性框 | 1 段 E 的真实问答摘录 + 1 段 D 的修改链摘录（各 1 个 box；发表前去标识/改写） |

现成实现：`analysis/figures.py`（招牌图 + 主 DV 已渲染）；网页版在研究员后台「📊 数据分析 → ③ 出图」。

## 6. 分析管线现状（A6 v3）

> ⚠️ **2026-08-03 尽职审计推翻了本节此前的「全部就绪」结论**：主分析 LMM 在 6 个终点里 5 个直接抛
> `LinAlgError` 且被 `except` 吞掉（只打印一行「LMM 失败」后继续跑 Wilcoxon，看起来像有结果）；
> H4 的 TOST 从未接进主流程、`prereg.SESOI` 从未被 `stats` import；`make figures` 只出得来一张图；
> 面板每次重算都静默抹掉 embedding 保真腿。**均已修复**，并新增回归门禁 `make smoke`
> （合成 N=36 跑完整条链 + 23 项断言，含试测规模 N=4 与 E 臂缺失）。
> **就绪度以门禁输出为准，不要再凭本表判断。**

| 模块 | 作用 | 状态 |
|---|---|---|
| `textstats.py` | 文本统计基元 | ✅ |
| `v3.py` | 结构完整度（含 `spec_ok`＝15s且3镜达标）/ 多样性 / 逐镜头保真 / 版本演化 / 努力再分配 / **E 引导剂量** / 主观复合 | ✅ 门禁覆盖 |
| `events.py` | 事件层：问卷时长 / LLM 次数与错误 / token / 最长等待 / 续接次数 | ✅ 门禁覆盖 |
| `stats.py` | LMM（优化器回退链）+ E−D 主对比(Holm) + **H4 TOST** + Wilcoxon + **H5 复合剂量** + 三个复合 | ✅ 门禁覆盖 |
| `power_sim.py` | SESOI 先验功效（LMM 同构） | ✅ N=36 → 80% 功效 dz≈0.48 ⚠️ **novice 子集功效曲线待补**（B1 连带动作） |
| `embed.py` | 相对基线保真 Δ | ✅ 数学自测 + 题目错配守卫 + **基线模型一致性硬校验**；API 路径待真数据 |
| `figures.py` | 招牌图 + 两个主复合 + 多样性 + 图4 编码原料 | ✅ 门禁覆盖 |
| `norming.py` | 主题开放度 | ⚠️ **需重跑**（`data/baseline/` 未入库，gitignored） |
| `pilot_check.py` | 4 生死问题 🟢🟡🔴 + 后手分支 | ✅ 门禁覆盖 |
| `judge.py` | 盲评保真（OpenAI×3 + ICC(1)），次要 / 收敛证据 | ✅ 已对齐 B5；`make judge` 单独跑，**不在** `make analysis` 链里（要 API key） |
| `prereg.py` | **冻结常量单一真源** | — |

`make analysis` = v3 → events → embed → stats → figures；`make smoke` 回归门禁；`make pilot` 试测读数；`make help` 列命令。
⚠️ 旧 `analysis/metrics.py`（v2 HLZ 时代）已被 `v3.py` 取代，**收数验收后删**。
⚠️ **`make baseline` 必须用与正式采数相同的模型快照重跑**——Δ 的零点由它定义，是全流程里少数
**事后补不回来**的一件（数据采完，当时的模型可能已下线）。不一致时 `embed.py` 硬失败、
`scripts/deploy_check.py` 亮红灯。
✅ **A1 四维质量 rubric 已弃**（B5，2026-08-03）；H4 的质量 DV = `structural_completeness`
＝ mean(`parse_ok`, 分镜四字段齐全率, `spec_ok`)，**单一复合、只做一次 TOST**（SESOI 0.10 与之同为 0-1 比例，无多重性）。
