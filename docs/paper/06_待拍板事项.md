# 06 · 待拍板事项（采数前）

> 建立 2026-08-03，来源：旧 paper/8（开放项）+ paper/17（⏳待拍板）+ 第四轮审计残留 + **本次 paper↔docs 一致性核对新发现的冲突**。
> **只列还没定的**。已定的进 [10_核心决策留痕](10_核心决策留痕.md)。
> 顺序 = 阻塞程度。①–⑧ **已全部拍板**（2026-08-03）；剩下的 ⑨⑩ 是可延后的工程取舍。

---

## ✅ ① novice 定位 —— **已拍板（2026-08-03）**

> **招募端不设门槛（随机找人）；`novice` 子集 = 事前确定的主分析人群；全样本作稳健性；
> 明显有创作经验的被试另作「経験あり vs なし」对比 / 调节分析。**

已落实：`analysis/prereg.py` NOVICE_DEF 注释、`docs/index.html` 手法页、[02 §4](02_实验设计.md)、[04](04_假设与分析计划.md) H8。

**⚠️ 随之而来的两个连带动作（还没做）**：
1. **功效必须按 novice 子集重算**（子集 N 更小 → 需要更大 dz 才有功效）。`power_sim` 要加子集功效曲线。
2. **超招募到「子集也达标」为止**，不是「全样本 36 就停」。若试测实测达标率偏低（`pilot_check ③` 🔴 = <40%），有两条退路：
   - 招募端加软门槛（广告直接面向「从没做过视频/分镜、没相关专业背景的人」——**杠杆最大**）；
   - 放宽 novice 定义为 **4-of-5**（在冻结时确定，不能看了数据再改）。

---

## ✅ ② SESOI —— **已锁定（2026-08-03）：`prereg.SESOI = 0.10`**

单位 = **DV 原始单位**；H4 主质量 DV = 结构完整度（0-1 比例）→ **0.10 = 10 个百分点**。
读法：「E 的结构完整度比 D 低 10 个百分点以内 → 质量无实质损失」。
取值理由与实测验证见 [05 §1.5](05_分析计划冻结与试测决策树.md)（0.05 过窄会自断非劣性主张；0.15 过宽）。
`stats.tost` 已实测：真差 3 个点 → 判等价 ✓；真差 20 个点 → 判不等价 ✓。

⚠️ **连带待办**：`power_sim` 要按 `dz ≈ SESOI / SD` 报**功效曲线**（SD 未知），且**主分析人群 = novice 子集（B1）→ 需另算子集曲线**。这条和 B1 的连带动作是同一件事，等你说做我就做。

---

## ✅ ③ 预注册 —— **已拍板（2026-08-03）：不做第三方预注册**

改为**内部冻结**：`prereg.py`（含已锁的 SESOI）+ 试测决策树 + 假设表，采数前一次性固定、`as_dict()` dump 成带 hash 的文件 + git commit 留档。
**代价（已知情）**：强度弱于 OSF；⛔ 论文/发表**不得写 "preregistered" / 「事前登録」**，只能写「分析计划在采数前确定并纳入版本管理」；Limitations 加一条；答辩被问 HARKing 时展示冻结产物 + git 历史并坦承弱于预注册。
✅ **`docs/index.html` 的「事前登録」措辞已全部改掉**（結び页 → 「解析計画を凍結」、分析页 ×2、手法页）。
详见 [05 §1](05_分析计划冻结与试测决策树.md)。

---

## ✅ ④ 伦理审查 —— **已拍板（2026-08-03）：不提交伦理审查，但同意书明示数据会给 OpenAI**

已落实（三语同步，`i18n/locales/{ja,zh,en}.json` `consent.body` 新增段）：

> **外部AIサービスの利用**：入力された文章（アイデア・修正の指示・自由記述）は、絵コンテを生成するために **OpenAI 社（米国）の API** へ送信されます。API 経由のデータは既定でモデルの学習には使用されず、不正利用の監視のために最大30日間保持されたのち削除されます。**氏名・学校名・実在の人物が特定できる情報などは入力しないでください。**

**⚠️ 公开前请自行复核一次**：上面的「学習に使用されない / 最大30日保持」是 OpenAI API 现行政策的通行表述，**政策会变**——采数开始前到 OpenAI 官方数据政策页核一遍，措辞对不上就改这段文案（改 `consent.body` 三语即可，无代码依赖）。
**可选加固**：申请 OpenAI **zero-retention** 端点，则「最多保留 30 天」一句可改为「保持しない」。

---

## ✅ ⑤ A1 质量 rubric —— **已判定（2026-08-03，用户授权我定）**

> **A1 四维质量 rubric（景别运用 / 节奏转折 / 视听语言 / 情绪锚点）：❌ 不做。**
> **`scripts/judge.py` 的盲评保真：✅ 保留，作次要 / 收敛证据。**

**⚠️ 先更正一处陈述**：旧文档说「judge.py 缺盲评保真模式、提示是中文、仍写异家族协议」——**读了文件，这些已经不成立**。`scripts/judge.py` 现状是：只做盲评保真（不含质量评分模式）、**提示已日语化**（JP3）、**OpenAI 同型号 ×3 + ICC(1) 自一致性**（异家族协议已作废并写明）、有 `--selftest` 桩自测、按 `(participant_id, round_idx, rep)` 断点续跑且 `PARSE_FAIL` 会重试。**它是对齐现行口径的，不需要重写。**

**所以真正要决的只有 A1 那部分，判定：弃。**
1. 四维全是**审美判断**，正是 TTCW（CHI 2024）实证 LLM 与专家**相关接近零**的地方；
2. 质量已由**结构完整度（客观下界）+ TOST 非劣**覆盖，加一个不可靠的审美分**反而削弱**「创意质量无法纯客观测，故退守不劣性」这条诚实论证；
3. 它不进任何主复合，做完只是次要栏一行数字；
4. 幻灯片对外口径本来就是「品質 = 構造的完全性」。

**而盲评保真留着，因为**：它判的是「这份 3 镜脚本多大程度实现了这句创意」——**结构化比对**（Zheng 2023 适用域），不是创意评分（TTCW 禁区）；且**已实装、已自测、边际成本≈0**，真数据到手跑一次就是保真三角之外的一条独立旁证。定位：**次要 / 收敛证据，不进主复合**。

**动作**：`scripts/judge.py` 的横幅已更新（去掉过时指针、写明 A1 已弃）。质量四维不再出现在任何计划里。

---

## ✅ ⑥ `MIN_INTENT_CHARS` —— **已拍板（2026-08-03）：10 → 8**

`core/config.py: MIN_INTENT_CHARS = 8`。理由：说明页已写「只给基调也行、长短随意」，10 字下限会拦掉「明るい話にしたい」这类**我们主动邀请的输入**；8 仍非零，空/单词创意不会让保真基线无锚。
三语错误文案 `errors.intent_too_short` 带 `{n}` 占位符，自动跟随，无需改 i18n。
试测时仍看 intent 长度分布与被拦次数（`pilot_check` 可算），确认 8 是否还需微调 —— **但冻结后不再改**。

---

## ✅ ⑦ 模型可复现性 —— **已实测 + 已落地（2026-08-03）**

**实测结论（gpt-4o-mini，真实脚本 prompt，4 组参数 × 各 5 次）：`seed` 救不了逐字复现。**

| 参数 | 5 次里有几种不同输出 |
|---|---|
| temperature=0.8，无 seed（原状） | **5 / 5** |
| temperature=0，无 seed | **5 / 5** |
| temperature=0，seed=12345 | **5 / 5** |
| temperature=0.8，seed=12345 | **5 / 5** |

四组 `system_fingerprint` 全部相同（`fp_5d8d1116aa`），即**后端没变仍然不可复现**——OpenAI 的 `seed` 官方就只承诺 best-effort，批式 GPU 推理的浮点非结合性使温度 0 也不确定。

**因此目标从「输出可复现」改为「配置可复现 + 漂移可检测」，已实装：**

1. **模型钉死日期快照**：`gpt-4o-mini` → **`gpt-4o-mini-2024-07-18`**（`.streamlit/secrets.toml` + `.example`）。这是**真正重要的一条**——浮动别名会在采数中途被 OpenAI 重新指向新模型，那才是真混淆。`deploy_check.py` 已有该检查。
2. **每次调用发送 per-trial 派生 seed**：`llm._seed() = (participant_id×100 + round_idx) % 2^31`。用派生而非全局常量，避免所有被试被同一个 seed 推向同一样本（那会人为压缩被试间方差、污染 H6）。
3. **`system_fingerprint` + seed 落进 `llm_done` 事件**（`payload.repro`）——采数中途 OpenAI 换后端时**事后可检测并如实报告**。
4. **temperature 维持 0.8**（创作任务需要；既然确定性拿不到，调 0 只会改变产物性质而无收益）。分析侧的 judge / embedding 另用 temperature=0。

**已验证**：真机调用 seed=702 正确送达、fingerprint `fp_b1b99e3b92` 正确落库、引导 JSON 5 问正常生成；E2E（含 resume/redo）全绿。
**写进 Limitations**：「LLM 输出非逐字可复现（实测 seed 固定仍 5/5 相异）；我们钉死模型快照与参数并记录 system_fingerprint，以保证**过程**可复现与漂移可检测。」——这条实测本身可以进方法附录。

> ✅ **B4 复测已完成**（2026-08-03）：`gpt-4o-mini-2024-07-18` 与 `gpt-5.4-mini-2026-03-17` **都通过**全路径 × 真三题 × ja（脚本 3 镜/15 秒、引导 r1 5-7 问/三固定维/3-4 选项、追问 1-3 问、D 修订合规、秒数提问 0）。
> ✅ **稳定性实测已完成**（各 20 次）：引导 JSON 首次解析 40/40 成功 → **fallback 实际不会发生**；`parse_ok=0` 未出现。唯一差距是 gpt-4o-mini 有 ~5% 规格越界（2 选项 / 问数越界），gpt-5.4-mini 0/20。详见 [02 §8.2](02_实验设计.md)。
>
> ✅ **配图模型已定：`gpt-image-1-mini`**（既定事项，2026-08-03）——全 6 个图像模型实测，它是**最便宜的且差距决定性**（$0.0022/张，比第二便宜的还便宜 2.7 倍）、零安全拒绝（42/42）、线条最干净；`size`/`quality` 也已在最省档（更小尺寸不支持）。详见 [02 §8.4](02_实验设计.md)。
>
> ⚠️ **剩下的两个待办**：
> - **部署时把 OpenAI 配置移到 `api_configs[0]`**（现在第 0 个还是 dev 网关 KAT-Coder，所以 `deploy_check` 该项仍红——这是正确行为）。
> - **正式采数前把文本模型换成 `gpt-5.4-mini-2026-03-17`**（现在测试期用 4o-mini）。`secrets.toml` 里已备好该配置块，移到第一位即可；`deploy_check` 会在模型不是它时亮 🟡 提醒。选型依据 → [02 §8](02_实验设计.md)。

---

## 🟠 ⑧ 部署硬门禁（`scripts/deploy_check.py`）

当前读数（2026-08-03）：**🔴2 / 🟡1 / 🟢3**

| 灯 | 项 | 要做什么 |
|---|---|---|
| 🔴 | 研究员密码 | 仍是弱口令兜底 `nova` → **在 `secrets.toml` 设强 `researcher_password`**。不改的话任何人能进后台看全部数据、还能看到研究目的（=去盲） |
| 🔴 | 正式模型置顶 | `api_configs[0]` 还是 dev 网关 → **把已钉快照的 OpenAI 配置移到第一个**。（快照本身已钉好，移位即变绿） |
| 🟡 | config runOnSave | `.streamlit/config.toml` 的 `runOnSave=true` 是 dev 设置，生产建议关 |
| 🟢 | 空库起跑 | 已无 participants 行；正式开跑前再确认 `data/archive/` 已收走开发库 + 旧 csv |
| 🟢 | 数据 gitignore | `data/*.db` 已忽略 |
| 🟢 | 备份脚本 | `scripts/backup_db.sh` 就位 —— **但还要自己确认已进 cron（每日 + 每场后）且异地一份** |

**闸门外还有一项 deploy_check 查不到的**：**HTTPS**。被试要填人口学与自由文本，公开链接必须走 TLS（反代 Caddy/nginx 配证书，防火墙挡直连 8501）。

**还需要你给 3 个信息**才能把 `DEPLOY.md` 的 systemd + Caddy 占位填成可直接用版本：**发行版 / 域名 / 是否有 sudo**。

---

## 🟡 ⑨ 第四轮对抗审计残留（16 条 low，均已对抗验证属实，涉设计取舍）

### 数据口径（涉分析定义，建议确认）
- `views/_postgen.py:56` **删空编辑框后提交**：现在静默回退到上一 AI 版（不落 `user_edit`、不计手改），**违反快照硬规则的精神**。二选一：**禁止空提交**（提交处报错拦）／**当一次 user_edit 计入**。
- `core/shots.py:100` 守卫1：shot_type 恢复进 `_FIELD_RE` 后，「无编号 + 景别在时长前」的分镜整体解析失败（诚实 `parse_ok=0`）。这是刻意「诚实失败胜过静默错切」；若试测实测 LLM 输出漂移多可放宽（守卫1 只认 visual/audio/duration 类字段）。
- `views/guidance.py` `guidance_answer_saved` 在 E-final 失败重试/回看时对同一问**重复落库** → 分析取 `(attempt,q)` 末条即可，或加幂等。

### 健壮性（防脏数据；属 CLAUDE.md §2「要不要为不可能场景加防护」的边界）
- `core/db.py:181` CG1 计数用 `json_extract`：若任一 passed 行的 `screening_json` 是非法 JSON（手改库/导入脏数据/写入中断）→ 整条 COUNT 崩 → **阻断所有新被试入库**。一行可修：`AND (json_valid(screening_json)=0 OR COALESCE(json_extract(...),0)!=1)`。正常流程不会触发。
- `core/db.py:141` 唯一索引建失败**静默 `pass`**：若库在建索引前已含重复 (pid,round) 行 → 索引永不建 → `INSERT OR REPLACE` 退化为普通 INSERT 静默产生重复行。真实新库不可达。→ 建议至少在 `monitor_panel` 暴露「唯一索引缺失」告警。
- `core/db.py:136` 迁移循环吞所有 `OperationalError`（可能掩盖非「列已存在」的真错）→ 可只吞 `'duplicate column'`。
- `core/state.py:165` `_attempt_resume` 在 `topics < N_ROUNDS` 时不恢复身份即返回 → 极低概率让被试重过筛占第二个 seq → 可改为仍恢复身份 + 置等待/错误态。
- **#22 resume 快照 round_plan**：`_attempt_resume` 从 `topics.json` **实时重建** round_plan，采数中途改题会**静默重配**已在进行的被试。修法：`begin_rounds` 时把 round_plan 快照到参与者行，或 hash topics.json 变了即拒/标记。（触碰稳定路径，故未擅动；**采数期间禁止改 topics.json** 是零成本替代。）

### 线程 / 进度条（`_run_with_progress`，已实测当前 Streamlit 版本能用）
- worker 线程与主线程共享同一 `ScriptRunContext`（官方不支持，但已小心设计成无重叠写）→ 求稳可重构：worker 只做纯网络 I/O，api_key/secrets 进线程前读出以参数传入、usage 经 result 返回。
- 忙等循环遇 rerun（StopException）会跳过 `join()/bar.empty()` → 留最长 120s 的孤儿 daemon 线程 → `try/finally` 兜底。
- `dev_smoke_e2e` 打桩替换了 LLM，跨线程 session_state 路径未被覆盖 → 可加定向单测锁契约（仅测试，无生产风险）。

### 命名 / 可读性 / 效率
- `r_llm_wait_pre` / `r_llm_wait_post` 命名暗示互补、实为非对称语义 → 建议改名 `r_llm_wait_e_pregen` / `r_llm_wait_postscript`，或在 DEFAULTS 注一句「三者非划分」。
- `views/_streaming.py` `call_llm_json` 用字符串 `"RETRY"` 当哨兵，与 `Optional[dict]` 注解矛盾 → 改 Enum/`object()` 哨兵 + 更新注解。
- `core/db.py:247` `insert_event` 每次新建连接 + PRAGMA（高频事件）→ 事件量大时改持久连接。
- `views/questionnaire.py` `render()` 内对同一终稿多次 `parse_shots` → parse 一次传入复用。
- i18n 4 个死键 `researcher.n_{participants,passed,done,trials}` → 确认不复用则三语同删。

---

## 🟡 ⑩ 可延后（不影响被试跑通，数据洁净/边角）

- **E 引导数量约束只靠 prompt**：固定 3 维 / 5-7 问 / 追问 1-3 问 / 每问 3-4 选项**没有实时校验**，模型偏离会原样呈现；问答数据已全落库可事后审计。加校验有误杀风险（偏离→误触发 fallback），**暂观察试测实测**。
- **E 追问 prompt 不带历史问题** → 追问轮可能重复问过的问题（概率低，模型能看到当前稿）。
- **LLM 返回空串（无异常）时无错误提示** —— C/D 只剩裸重试按钮、E 终稿/D 修改静默无反应（罕见）。
- **多标签页同 token 并发无守卫** —— 两个标签页各自推进同一被试会互相覆盖（单人单坐的实验，现实概率极低）。
- **final_survey 提交 → 置 done 之间断线**，续接会让被试重填总问卷（秒级窗口）。
- **杂项 nit**：DB 连接用完没显式关；JSON 模式没用官方 `response_format`；逐镜头三标签语义略重叠；「15秒・3カット」硬/软约束没向被试讲清；历史面板默认展开占首屏；进度条在 final_survey 阶段不显示（可接受）；consent + 管理员两个语言器同时渲染显示可能不一致（仅管理员）；`reset_for_next` 后研究员面板仍开着；`load_topics` 对损坏文件静默回退种子主题（不崩但不告警）；C/D 生成中杂散 rerun 会整段重新生成（无幂等守卫，未观测到实际触发）。

---

## 📌 已明确「不再讨论」的（别再提议）

- **不做外部真人评审 / 人评效度锚**（含众包）——2026-07-02 拍板④ + 2026-07-18 再确认。理由：招人评审有找不齐/评分漂/拖进度的不稳定因素。
- **不加反向/默许控制的问卷题**——2026-07-19 决定。被试内差分已扛主威胁；敷衍剔除已由 `attention_raw` + `straightline` 覆盖。
- **不强化 C 条件**（多次生成 / pick-from-3）——理由见 [02 §1](02_实验设计.md)。
- **不做剧本 JSON 化**——剧本要「流式生成 + 直接手改」，改 JSON 会废掉流式直出和文本编辑；对人用 markdown、解析器兜底。
- **`MIN_INTENT_CHARS` 的日语字数门槛问题**（JP5）本身不关注，只剩 ⑥ 的文案矛盾要处理。
