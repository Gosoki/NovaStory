# 07 · 采数前执行清单（部署 · 合规 · 监控）

> 来源合并：旧 paper/13（数据管理与被试合规，全文保留）+ paper/8 §🎓（答辩前必修的执行侧）+ `DEPLOY.md`。
> 定位：**操作性规格**。从「设计」跨到「数据」的唯一剩余门槛就在这一页。

---

## 0. 一句话

**代码已就绪，卡点全在部署 + 合规 + 拍板。** `scripts/deploy_check.py` 跑到**全绿**才能公开链接。

> **开跑前的四条命令**（2026-09-01 起）：
> ```bash
> make robust      # 出事时扛不扛得住:并发/断网/刷新/脏数据/后台线程 → docs/paper/12
> make smoke-e2e   # 正常全流程走得通
> make smoke       # 分析链
> .venv/bin/python scripts/deploy_check.py   # 部署硬门禁(含新增的题库闸门)
> ```
> 三条链全绿 + 闸门无红灯，才发链接。**改过任何代码都要重跑**。
> 现场运维（模型探测、盯监控三个数、采数期禁改题库、WAL 热备）→ **[12 §3](12_实测就绪性验证_20260901.md)**。

## 1. 部署硬门禁

| 项 | 现状 | 工具 |
|---|---|---|
| 研究员密码 | ⚠️ **弱口令 `nova` 兜底**——必须在 `secrets.toml` 配强 `researcher_password`，否则任何人可入后台 = 数据泄露 + 研究目的暴露（`core/config.py:35`） | `deploy_check` |
| 模型置顶 | secrets 首个 `[[api_configs]]` 必须是 **OpenAI**（正式实验全程 OpenAI，B9），引导步跟随主配置 | `deploy_check` |
| **模型快照 + 选型** | 测试期 = `gpt-4o-mini-2024-07-18`（已钉快照）。⚠️ **正式采数前换成 `gpt-5.4-mini-2026-03-17`**（选型实测见 [02 §8](02_实验设计.md)；`secrets.toml` 已备好配置块）**并移到 `api_configs[0]`**——两件事没做完该项都不会绿。`gpt-image-1`（配图）无日期快照可钉，可换 `gpt-image-2-2026-04-21`（先看画风），非 DV | `deploy_check` |
| **可复现性** | ✅ 每次调用发送 per-trial 派生 seed，seed + `system_fingerprint` 落 `llm_done.payload.repro`。⚠️ **输出非逐字可复现**（实测 temp=0+固定 seed 仍 5/5 相异），目标 = 配置可复现 + 漂移可检测 | `core/llm.py` |
| **配图模型** | ✅ `gpt-image-1-mini`（2026-08-03 选型实测：比原 gpt-image-1 快 30%、便宜 5 倍、线条最干净）。⚠️ 图像模型**无日期快照可钉** = B7 的已知例外；每张图已归档，可事后复算风格指标查漂移 | `core/imagegen.py` |
| **Embedding** | ⚠️ **未就绪**：只接了 OpenAI 一路，凑不齐「≥3 个异构日语模型」。保真 Δ 是**采数后**才算的，不挡采数，但**分析前必须补一个开源模型**（multilingual-e5 / bge-m3） | `analysis/embed.py` |
| 空库起跑 | 归档开发库 + 旧 `data/experiment_results.csv` 到 `data/archive/`，从空库开始 | `deploy_check` |
| HTTPS | 被试填人口学与自由文本，公开链接**必须走 TLS**；反代（Caddy/nginx）配证书；防火墙挡直连 8501 | `DEPLOY.md` 占位 |
| 备份 | 每日一次 + 建议每场被试完成后一次；WAL 安全 `.backup` + 完整性检查 + 轮转；**备份目录与主库分盘/异地至少一处** | `scripts/backup_db.sh`（cron） |
| gitignore | `data/` 与 `backup/` 均已忽略——被试数据永不进 git | 已就位 |

> **还差 3 个信息**才能把 `DEPLOY.md` 的 systemd + Caddy 占位填成可直接用版本：**发行版 / 域名 / 是否有 sudo**。

```bash
# 就绪闸门（有红灯 exit 1）
python scripts/deploy_check.py
# 备份
bash scripts/backup_db.sh
```

## 2. 数据管理与被试合规

### 2.1 数据都有什么（敏感度分级）

| 数据 | 存哪 | 敏感度 | 备注 |
|---|---|---|---|
| 人口学/筛查（年龄段·专业·AI 使用等） | `participants.demographics_json` / `screening_json` | 低-中 | **以 index 存储、语言无关**，非自由文本；最小化采集 |
| 基线协变量（aiexp/trust/own_trait） | 同上 | 低 | 刻度值 |
| **被试自由文本**（创意 intent / D 的反馈 / 手改稿 / final_survey 留言） | `trials.*` / `participants.final_survey_json` | **中-高** | 可能无意中含可识别信息 → §2.4 |
| AI 生成的每版脚本 | `trials.script_versions` | 低 | |
| 量表作答 / 逐镜头标注 | `questionnaires.*` | 低 | |
| **被试选择的语言** | `participants.lang` | 低 | ja/zh/en，同意页选一次。已进分析管线与监控台（2026-08-03） |
| 行为事件流（时间戳/动作/token） | `events` | 低 | |
| 续接句柄 token | `participants.token` | 中 | 进网址 `?t=`，是会话恢复凭证，非身份 |
| 完成码 | `participants.completion_code` | 低 | 8 位随机大写字母数字(去 0/O/1/I),`secrets` 生成,签发一次不重签 |
| 分镜插图 | `data/storyboard_images/{被试}_{轮次}/{attempt}/` | 低 | 已 gitignore;`backup_db.sh` 会打包 |
| **自愿留下的联系方式**(邮箱 / 是否要视频 / 备注) | `participants.contact_json` | **高**(唯一一列直接个人数据) | 完成页自愿填写,同意书已写明;后台表格与 CSV **不导出**此列(`views/researcher.py`) |
| LLM 调用日志 | `data/llm.log` | 低 | 被试 id + 计时 + 错误 repr(不含正文);已 gitignore |
| judge 评分记录 | `data/analysis/judge.jsonl` | 低-中 | PARSE_FAIL 时保留模型回复原文 |
| 备份 | `backup/*.db` `backup/storyboard_images-*.tar.gz` | 同主库 | 保存期限与删除承诺同样覆盖这里 |

**不采集**：姓名、学号、IP（Streamlit 默认不落库）、任何直接身份标识。**邮箱只在被试自愿填写完成页表单时**存入 `contact_json`（2026-09-02，见 13 §0.7 / 10）。⚠️ 反代（Caddy/nginx）的访问日志默认会记 IP 与含 `?t=` 续接句柄的 URL —— 部署时要么关掉请求行/查询串的记录，要么把这一条写进「采集」并定保存期。
`participant_id` = 自增整数，与真人身份之间**无映射表**（**匿名**而非假名）。

### 2.2 存储

SQLite 单文件 `data/novastory.db`（WAL 模式），自有服务器原地持久，不用外部 DB。

### 2.3 ⚠️ 第三方数据流出：被试文本会发送到 OpenAI

**这是最容易被忽略的合规点。** 被试写的创意、手改稿、D 的反馈，都会作为 prompt **发送到 OpenAI API**（`core/llm.py`）。即：

- 被试的原创文本会**离开你的服务器、经过第三方（OpenAI）**；
- **同意书必须明确告知**「你输入的内容将由外部 AI 服务（OpenAI）处理以生成脚本」；
- 参考 OpenAI API 数据政策（API 数据默认不用于训练、有限期留存），写进同意书 / 数据管理说明；
- **APPI（日本个人情报保护法）数据越境**：同意书须达越境提供的粒度——**国名 / 保留期 / 是否用于训练**。
  ✅ **已实装（B4, 2026-08-03）**：`consent.body` 三语新增「外部AIサービスの利用」段，写明 **OpenAI 社（米国）**、默认不用于训练、最长 30 天后删除、并要求被试勿填可识别信息。
  **可选加固**：申请 **OpenAI zero-retention 端点**，则「最多保留 30 天」可改为「保持しない」。
- **伦理审查**：✅ **已拍板（B4, 2026-08-03）不提交伦理审查**；上述告知义务照做（与是否送审无关）。

### 2.4 自由文本的可识别信息风险

被试的创意/反馈/留言是自由输入，**可能无意中写入真实人名、校名、具体经历**。处置：
- 同意书说明「请勿填写可识别他人或自己隐私的真实信息」；
- 分析/发表前对自由文本做一遍**人工去标识扫查**（尤其要进论文定性框的摘录）；
- 定性摘录发表前改写/打码。

### 2.5 数据用途与保留

- **仅用于本研究**（学位论文 + 投稿 + 公开数据集若做则匿名后）。
- 不完整数据（未走到 trial_submit / final_survey 的 in_progress 被试）**弃用不进分析**，但匿名记录可留档。
- 保留期：按学位/投稿要求（通常成果发表后数年），到期后处置。

### 2.6 采数前 checklist（数据侧）

- [ ] 空库起跑（归档开发库 + 旧 csv）— **用 `DEPLOY.md §3` 的整库归档，不要 DELETE**
- [ ] 强 `researcher_password` 已配 secrets（≥12 字符；闸门按这个判红）
- [x] 每日备份 cron 已跑通 — ✅ `0 3 * * *`，已在纯 cron 环境逐字验证；**异地一份仍待办**
- [x] HTTPS 已生效 — ✅ `https://test.jp.wuzuxi.com`（nginx + HTTP/2 + gzip）
- [x] 开机自启与看门狗 — ✅ crontab `@reboot` + `*/5` 拉起（不装 systemd 的替代，2026-09-06 拍板）
- [x] 同意书含：**外部 AI（OpenAI）处理 + APPI 越境粒度**、勿填隐私、可随时退出、数据用途与匿名 —— ✅ 已实装三语 `consent.body`（B4, 2026-08-03）
- [ ] **公开前复核一次** OpenAI 官方数据政策页，确认同意书里「学習に使用されない／最大30日保持」的措辞仍属实（政策会变；改 `consent.body` 三语即可）
- [ ] 自由文本去标识流程就位（分析/发表前）

## 3. 采数期监控（研究员后台「🗂 数据管理·监控」）

**每天看的：**
- 概览卡片：完成 / 进行中 / 筛除 / **novice 占比** + 目标 N 进度条
- **各 seq 完成数**（18 格补全）+ 目标 N/18 提示 → 判断**超招募停止规则**是否达标
- 条件×题目计数平衡热力表、每日完成数、各条件主观均值
- **数据健康块**：解析失败率（`parse_ok=0`）/ LLM 报错率 / 引导降级率（`fallback`）/ 生成延迟中位·95%
  ⚠️ 绿色进度条会掩盖 `parse_ok=0` 腐蚀，**必须单独看数据健康块**。
- **语言构成**：ja = 日本队列、zh = 中国队列，两者都是正式数据（2026-09-06 方向变更）；只有 **en** 才告警。
- **进行中的会话**（2026-09-05 新增）：编号 / 序号 / 开始时间 / 最后活动 / 已交轮数 / **释放**。
  序号在通过筛查那一刻就被占住且不会自动回收——确认某人不会再回来时点「释放」，序号让给下一位
  （该行标 `dev`、数据保留但不进分析）。⚠️ 对方拿着 `?t=` 回来仍能做完，所以先看「最后活动」。
- **未闭合调用**（2026-09-05 新增）：`llm_start` 减去 `llm_done`/`llm_error`。**失控重试只有 start、
  一条 error 都没有**，旧的「LLM 报错率」对此显示 0% 完美——生产库里真发生过 86/111。超 25% 会告警。
- **配图成功率**（2026-09-05 新增）：429 被 `except pass` 吞掉，被试看到的是空白画框而面板此前一字不提。
  低于 80% 告警，多半是撞了 `gpt-image-1-mini` 的分钟限额。
- **API 用量**（2026-09-07 新增）：文本调用次数 / 输入·输出 token / 配图张数 / 按 C·D·E 拆分 / 人均。
  人均只按**走完的人**算。配图不记 token，按张数计（选型实测约 $0.0022/张）。

**试测期额外监控**（B3）：
- 轮数 / 净用时分布（验证 10-15 分钟软目标是否现实）
- **E 第 1 轮 AI 代答率 > 50% 预警**（若过高，补充维上限 4→2）
- D 的 AI 通道使用率（= D 地板效应的先兆，见 [05 §2①](05_分析计划冻结与试测决策树.md)）
- 引导步实测延迟

## 3.5 现场采数（中国中专/职校，2026-09-06 方向变更后新增）

线上滚动招募之外，这一路是**讲座结束后当场用学生自己的手机做**，坑和线上不同：

- **入场时间必须错开。** 一屋子人同时点开链接，会在第 1 轮结束时同步撞上 `gpt-image-1-mini` 的
  分钟限额（30 人齐发约 45 张/分钟）。分两批、间隔 8–10 分钟，零代码成本、收益最大。
  代码侧已加全局并发闸（同时最多 8 张在飞），但档位太低仍不够 —— **先去 OpenAI 后台确认 image 的 IPM**。
- **同一台设备轮流给多人做 → 必须每次清存档。** 续接靠浏览器 localStorage 里的 `?t=` 链接
  （`core/state.remember_resume_url`），不清的话第二个学生会看到「继续上次未完成的作答」并被引进
  前一个人的会话。清法：研究员面板点一次「初始化所有记录」（`forget_resume_url`）。
  学生用自己手机就没这个问题。
- **未成年同意**：`age_idx` 存的是档位序号，事后**分不开 11–17 与 18–20**，同意书里也没有年龄资格条款
  （写了就与题面矛盾）。招募侧口径挂在 `06 ⑱`，**现场组织前必须有答案**。
- **网络**：全程要联网（每轮至少一次 LLM 调用 + 配图）。校园 WiFi 或流量都行，
  但断线会让当前轮从头再来（未提交的创意与脚本会丢），事前说一句能省很多麻烦。
- **讲座材料**：`docs/deck_teens.html`（18 页中文幻灯片）+ `docs/deck_teens_script.md`（讲师稿）。
  ⚠️ 讲师稿 §3 有**禁用清单**：不能拿三个真题目当例子、不能说哪条路更好、不能把「先出再改」说成浪费 ——
  否则直接给因变量施加需求特征。灵魂句「与其做完了返工，不如动手前问清楚」**不能对被试讲**。
- **语言**：中国队列用 `zh`（现为默认）。**日本队列不与中国队列同期开放**（见下条），
  轮到日本队列时才把 `i18n/translator.py` 切回 `ja` 先头 —— 闸门的「既定言語」项会提醒。
- 🔴 **两队列必须分批次、不得同期开放**（2026-09-07 拍板，理由见 `docs/paper/10` 同日条目）。
  Williams 序号是全局补空缺、不按 `lang` 分组，同期开放会让两队列各自只摸到一半的条件顺序
  （`[8,4,8,4,8,4]` vs `[4,8,4,8,4,8]`），队列内部的残留效应平衡失效。分批次跑则各自 `[6,6,6,6,6,6]`。
  实操三条：**① 日本队列开始前，中国队列必须完全停止**（不能事后回头补几个中国被试）；
  **② 每队列的完成数尽量落在 18 的整数倍**（36 = 2×18；实收 30/33 会出现 `[6,6,6,6,3,3]` 一类轻度
  不平衡，不致命但要在论文里如实报告实际顺序分布）；**③ 两批之间绝对不改代码、prompt、模型、`topics.json`** ——
  队列与时间、模型版本已经完全共线，任何改动都会让「国别差异」和「版本差异」永久分不开。
  漂移的唯一探针是后台的 `system_fingerprint`→`served_model` 告警（`views/monitor_panel.py:249`），
  **两批采数期间都要盯**。

## 4. 执行顺序（从现在到出结果）

```
拍板 06 的 ①–⑧  ✅ 已完成
  → 换正式模型 gpt-5.4-mini-2026-03-17（移到 api_configs[0]）        ✅ 已完成
  → 【拍板】**两队列统计形式 (a)分开/(b)合并/(c)主+验证**   ⏳ 冻结的前置
     （目标 N = **全样本** 36/队列，2026-09-07 人群反转后已定；`NOVICE_MIN_CRITERIA=5` 不变，仅服务探索性切分）
  → 🔴【因中国队列在前而升级】中文题面的开放度 norming（`06 ⑰-3`）
     —— 必须在**第一批**采数前解决，不能等日本队列（与人群无关：它是拉丁方分离题目主效应的前提）
  → 🟠 中文新手小测的等价题难度（`06 ⑰-2`）—— 2026-09-07 人群反转后只影响探索性分层，不再阻塞；正式降级待拍板
  → 【拍板】四条分析口径（Δ 零点公式 / 整稿回退行 / 多样性等 n 化 /
            strip_format 口径）+ 复合 z 的参照人群                      ⏳ 冻结的前置
  → **make baseline**（正式模型，约 90 次生成）→ **make norming**（重测开放度）
  → deploy_check 全绿 + 备份跑通 + HTTPS
  → **冻结分析计划**（`make freeze`，方案 A「先验锁死」，05 §4，2026-09-01 拍板 2.7）
  → 小规模试测（只排雷，数据不入分析）→ make pilot（4 生死问题 🟢🟡🔴）→ 🔴 时按 05 决策树走**事先声明**的后手
  → 【试测后】归档试测库，从空库起跑正式采数（否则试测行会混进确证分析，13 §3.3）
  → 正式采数 **第一批：中国队列 36 人**（`zh`，现场讲座后当场做；跑完为止，中途不开日本入口）
  → 中国队列**完全停止** → 切 `i18n/translator.py` 到 `ja` 先头
  → 正式采数 **第二批：日本队列 36 人**（`ja`，滚动招募）
     ⚠️ 两批之间不改任何代码/prompt/模型/`topics.json`；每晚备份，盯监控台的模型漂移告警；
     研究员入口 = 网址加 `?admin=1`
  → make analysis（一发出全套结果 + 图）
  → 写作
```

> ⚠️ **`make baseline` 与 `make norming` 此前不在这张顺序表里，是漏的**（2026-09-07 补）。
> - **baseline** 是保真主复合里 embedding Δ 的零点，占该终点**一半权重**。没有它，`stats.build_composites`
>   会退回「主观三腿等权」并打印一句「非各半定义，结论须注明」——H1 的主终点之一就变成纯自评，
>   而答辩火力点恰恰是「没有外部真人保真锚」。且它**必须与采数同一个模型快照**，事后补不回来。
> - **norming** 是「三题开放度可比」的证据，而那是拉丁方能分离题目主效应的前提。
>   2026-09-01 与 2026-09-06 两轮改动之后，`02 §3` 那组数字量的已经是另一份题面，必须重测（见该处警告）。
>
> 两步都要真实调 API，但量很小：baseline 约 90 次生成 ≈ 6 万 token，约等于 24 位被试的文本用量。

## 5. artifact 包（边际成本低、直接提 rigor 评分）

发布时打包：**匿名数据 + `analysis/*` + prompt 全文 + 日语量表最终版 + codebook + `prereg.as_dict()` 的 hash 化 dump**。
