# 10 · 核心决策留痕

> 压缩自旧 paper/11（项目时间轴）与 paper/8（已结清）。**只保留仍有解释价值的关键拍板与方向转向**；
> 已完成的代码修复、审计过程、逐条 bug 记录一律丢弃（在 git 历史里）。
> 维护规则：发生 ①研究方向变更 ②实验设计重大调整 ③应用版本性重构 ④关键决策拍板 → 在**最上方**追加一条（日期 + 类别 + 一句话 + 受影响文档/代码）。日常小改不记。
> 类别标签：`方向` / `设计` / `代码` / `决策` / `文档`

---

## 2026-09-06 · `决策` **部署方式拍板:不装 systemd 单元,用 `scripts/start.sh` + crontab `@reboot`**

原状是 `systemd-run` 造的 transient 单元 —— 定义只存在 `/run`(tmpfs),**机器一重启就蒸发**,
且 `Restart=always` 只管进程崩溃、管不了单元本身消失。用户拍板不装持久单元,改用普通后台进程。

落地:新增 `scripts/start.sh`(start/stop/status/restart;走 `serve.py` 让预压缩生效;
pid 写 `data/novastory.pid`、日志 `data/serve.log` 超 20 MB 自动转存;端口被占时**拒绝启动并指明占用者**,
不去杀别人的进程)。开机自启改用 crontab `@reboot sleep 20 && cd <项目> && scripts/start.sh` ——
这补上了 transient 唯一真正致命的那个洞。`deploy_check` 的「运行时」项相应改写:
没有 systemd 单元时转而检查①进程在不在②`@reboot` 行是否存在且带 `cd`,两者齐备判绿。
实测:去掉 `@reboot` 会准确报红。**红灯由 3 降到 2**(只剩研究员口令与冻结产物)。

⚠️ 一并记为**已知接受的取舍**:以 root 运行、绑 `0.0.0.0`。前者要改需把仓库与 uv 解释器一起搬出 `/root`
(`.venv/bin/python` 是指向 `/root/.local/share/uv/...` 的符号链接,只搬仓库会 203/EXEC);
后者是因为 nginx 在另一台机器(10.0.0.1),绑回环会切断反代。详见 `DEPLOY.md §2`。

---

## 2026-09-06 · `方向` **被试扩到中国中专/职校学生，现场手机采数；新建面向该人群的中文讲座幻灯片**

拍板：去中国的中专/职校**现场招募并真实采集一次数据**，与日本被试**按 `lang` 区分**后分开或合并分析均可；
现场用学生**自己的手机**，讲座讲完当场做，**纯自愿无报酬**；面向未成年的同意事项**不写进幻灯片**。
这是 `02` 全文「被试＝日本人」假设的方向性变更 —— 代码侧 `zh` 全链路本就可用，**不阻塞**；
但 `lang` 进不进模型、`is_novice()` 的日语术语小测能否照搬、三题开放度的中文 norming 从未重跑，
全部挂在 [`06 ⑰`](06_待拍板事项.md)；未成年同意的招募侧口径挂 `06 ⑱`。

产物：**`docs/deck_teens.html`**（18 页浅色大字中文幻灯片，复用 `index.html` 的导航 JS 与版式原语，
补齐了原版三个坑：进度条/页码本来无样式、硬编码页索引、浏览器后退失效）+ **`docs/deck_teens_script.md`**（讲师稿）。

**讲座内容受实验设计反向约束**（写进讲师稿 §3 禁用清单）：不能拿三个真题目当例子、
不能把「先出再改」说成浪费、不能说哪条路更好 —— 否则直接给因变量施加需求特征。
灵魂句「与其做完了返工，不如动手前问清楚」**因此不能对被试讲**，幻灯片改为三条路等价呈现 + 「现在真没定论」。

同批 Playwright 实测（390×844 走完全流程）得到手机端结论：整体**基本可用、零横向溢出**，
但发现三处硬伤，**当天全部修掉并实测通过**：① 页顶下拉 = 刷新（加 `overscroll-behavior-y: contain`）；
② 输入框 14px 触发 iOS 自动放大（触屏设备提到 16px，桌面后台不变）；
③ **关标签页重开 = 新被试且吃掉一个拉丁方 seq** —— 带 token 的 URL 存 localStorage，
同意页出「继续上次」入口（`state.remember_resume_url` / `offer_resume` / `forget_resume_url`，三语文案）。
实现受两条浏览器约束，写进 docstring 别改回去：iframe sandbox 无 `allow-top-navigation`（不能自动跳转）、
srcdoc 里 `document.body` 是 null（不能在 iframe 内画 UI，条子得插进 parent 的 body）。
顺带把 `components.v1.html` 换成 `st.iframe`（前者 2026-06-01 起待移除，每次渲染刷一条 deprecation）。
回归 `make i18n / smoke-e2e / robust / smoke` 四条链全绿。
**恢复只在同一设备同一浏览器有效**，换手机仍会产生孤儿行，清洗口径待定（`06 ⑲`）。
**语言口径同日拍板**：「zh 也是正常数据，但要区分 zh 和 ja，以后做数据分析可以分开就行」。
据此把研究员后台与分析侧的语言告警改掉——旧口径把 `zh` 说成「研究员测试或脱离协议的会话」，
会把真被试标成脏数据；现在 `ja`/`zh` 都是正式队列、只以 caption 呈现构成，**只有 `en` 才告警**
（`views/monitor_panel.py` · `analysis/v3.py` · 三语 `monitor.lang_mix` · `robustness_check` 的断言从
「zh 触发告警」改成「ja+zh 不告警 / en 才告警」两条）。**统计形式仍待你定死**（各自独立分析 /
合并建模按 lang 分层 / 一地主分析另一地验证），选定后落进 `analysis/prereg.py` 并同步 `power_sim`
的两地目标 N → `06 ⑰`。

**时长口径拍板**：同意书三语维持「约 10–15 分钟」**不改**（用户判断实际就是 15 分钟左右），
幻灯片对学生也说 15 分钟；课表上留 20 分钟只是冗余、不对被试讲 → `06 ⑪` 已结。

---

## 2026-09-05 · `代码` **采数前就绪审查:测试脚本会偷偷打真实图像 API(已修);监控面板补上三个"看不见的失败"**

五路并行审查(25 agent,54 条 → 确认 17)。最严重的一条:`dev_smoke_e2e` / `robustness_check`
只打桩了 LLM,没碰 `imagegen`,而 `state._ensure_api_defaults` 会把 secrets 的 OpenAI 配置自动灌进
session_state —— 探针实测每跑一次 `ensure_started` 被调 **6 次**(每次最多 3 张图),而出图 worker 是
daemon 线程、脚本一结束就被杀,**磁盘上零痕迹却可能已经把请求发出去**。修:`imagegen` 加
`NOVASTORY_NO_IMAGES=1` 总闸 + 两个脚本各自置位;修复后探针为 0 次,生产出图不受影响。

监控面板补三项此前"结构上看不见"的失败:①**未闭合调用**(llm_start 减去 done 与 error)——
失控重试只有 start、一条 error 都没有,旧的错误率显示 0% 完美,而现有库实测 **77%**(86/111);
②**配图成功率**(429 被 `except pass` 吞掉,被试看空白画框,面板此前一字不提);
③`parse_ok` 为 NULL 不再被算作"解析成功"。会话列表时间统一 JST,与每日完成图口径一致。
另:部署闸门「冻结产物不存在」由 🟡 提为 🔴(它与机器基线同属事后补不回来的东西,不该 exit 0 放行),
增查 `UnitFileState`(装了持久单元却忘了 enable,重启后一样起不来);
`deploy/novastory.service` 模板的安装命令修正(`.venv/bin/python` 指向 /root 下的 uv 解释器,
照抄会 203/EXEC)。逐条见 `13 §⑦-R/S`。

---

## 2026-09-05 · `设计` **拉丁方序号改为「补空缺」分配,并给监控面板加会话列表 + 释放入口**

原来 `seq = (passed=1 且非 dev 的行数) % 18` —— 计数里没有 `status`,所以**通过筛查那一刻就占住一个格子,永不回收**:
关掉页面再也不回来的人照占。当时库里 13 人占了 0-12,实际走完只有 3 人,10 个格子被弃号锁着。
18 个 Williams 序列要平衡就得每格人数相等,弃号会让那些格子一直缺人。

改为 `seq = 用得最少的格子(平局取小)`。**无空缺时与旧逻辑逐个等价**(实测连续 20 人轮转完全一致),
区别只在释放之后:旧法会让下一个人拿到一个已被占用的号(重复),新法正好补上空缺,分布保持各格相差 ≤1。
配套新增 `db.release_participant(pid)`:把 `screening_json.dev` 置 1 并记 `released_at`,**不删行** ——
删行会算错后续序号、也毁掉已采数据;标记 dev 则让该行同时退出序号分配、`v3.included_participants`
与监控面板,与 devtools 注入行同一条排除路径。⚠️ 被释放者的 token 仍有效,回来还能走完,只是不进分析。
面板新增「进行中的会话」:编号/序号/开始时间/最后活动/已交轮数/释放(popover 二次确认),
并显示当前 WebSocket 连接数。i18n 三语各 +13 键。详见 `13 §⑦-Q`。
**顺带结论:招募量应按「完成 36 人」倒推**(36 = 18×2 才平衡),30 人完成会让序号 0-11 各 2 人、12-17 各 1 人。

---

## 2026-09-04 · `设计` **关掉 Streamlit 的「运行中整页变暗」——它会系统性污染 NASA-TLX 与挫折感评分**

被试反馈「配图生成时问卷也会暗下来」。查清是两条独立机制:① 分镜表占位高度按 `offsetHeight`
取整反复改写,下方问卷抖 1px(已修:加 1px 死区);② **真正的变暗**是 Streamlit 给每个
`.stElementContainer` 施加的 `opacity:.33`(带 .5s 延迟 + 1s 淡入)——fragment 自动轮询只标
该 fragment 内的元素(实测 1/13),但**被试每点一次量表选项都是一次全量 rerun**,整页被标 stale
(实测 12/13);配图生成期间 rerun 轻易超过 .5s 阈值,于是「答一题、整页暗一下」。
A/B 实测(把 rerun 拖到 2.5s 让淡入跑满):无覆盖时问卷区 opacity 掉到 **.33**,加一条 CSS 覆盖后全程 **1.00**。

**这是被试所见界面的变更,待你追认。** 判断利大于弊的理由:本轮问卷含 NASA-TLX 负荷题与挫折感题,
界面反复闪烁会推高这两项,**且闪烁频率与条件相关**(E 的分镜生成更晚、rerun 更慢)= 系统性污染;
而加载反馈没丢——画框在图片没好时一直显示「生成中」占位。撤销只需删掉 `app.py` 里那三行 CSS。
Streamlit 1.60 无官方开关可关此效果。详见 `13 §⑦-O`。

---

## 2026-09-03 · `代码` **第五轮全代码逻辑审计:基线生成与部署闸门加固;四条分析口径须在冻结前拍板**

10 路并行审计 + 对抗验证(28 agent)。原始 64 条 → 送验 18 → 确认 15、驳回 3,**验证后无 blocker/high**
(9 medium + 6 low),前四轮确已把严重问题清干净。已修六处工程缺陷,都不涉及口径变更:
① `baseline_gen` 空稿绝不落行(基线是 Δ 的零点且事后补不回来,一行空稿会把质心拉偏而下游全程沉默);
② 基线每行记 `lang`,三语文件同名不再互相混入;③ `deploy_check` 的基线闸门改逐行读,题数/份数/空稿/混语言各自红灯;
④ 题库闸门新增前 3 题两两查重(重复题会让每位被试在两轮拿到同一题,轮间不可比,且标题重复会让
`embed.title2idx` 静默塌键);⑤ 编辑框 `max_chars` 4000→20000(AI 稿被截断会被记成 user_edit,
伪造 `hand_edit_chars` —— 那是「努力再分配」主图的指标);⑥ 预压缩新鲜度改双向比较(降级 Streamlit 后会发旧 JS)。
**⚠️ 四条分析口径必须在 `make freeze` 前拍板**(采数后改 = 换口径):Δ 零点用质心还是余弦均值、整稿回退行
进不进保真主复合、多样性指标的等 n 化、`strip_format` 的两种文本口径。逐条见 `13 §⑦-N`。

---

## 2026-09-03 · `决策` **采数前状态归零:开发库整体归档、正式模型 `gpt-5.4-mini-2026-03-17` 置顶、反代开 gzip**

三件一起做,把部署闸门的红灯从 6 个压到 2 个。
① **开发数据归档**:9/01–9/03 试跑攒下的 8 位 participants / 16 trials / 16 questionnaires / 383 events
按 `DEPLOY.md §3` 整库移到 `data/archive/novastory.db.20260903_191041`(**归档不删除**,随时可查),
重启后自动新建空库,权限收到 600/700。第一位真被试将从 id=1 起。
② **正式模型置顶**:`api_configs[0]` 由 `KAT-Coder-Exp-72B-1010` 换成 `OpenAI · gpt-5.4-mini (FORMAL / pinned snapshot)`
= `gpt-5.4-mini-2026-03-17`(B9 拍板的正式快照)。⚠️ **`make baseline` 必须用这个快照重跑**,基线与采数不同源的话
embedding 腿(占保真复合一半权重)事后补不回来。
③ **反代 gzip 生效**:首屏 5.8 s → **2.4 s**。详见下条与 `13 §⑦-M`。
剩余红灯 2 个:研究员口令仅 6 字符;运行时仍是 transient 单元 + root + 绑 0.0.0.0。

---

## 2026-09-03 · `代码` **首屏加载:静态资源改发预压缩产物,线上字节降 69%;新增生产启动入口 `scripts/serve.py`**

Streamlit 1.60 出厂就让 `/static/` **绕过 gzip**(源码注释说是按局域网压测调的),跨境链路上这个默认正好反了 ——
实测首屏 122 个请求 2.50 MB 全是原文。新增 `scripts/precompress_static.py` 离线压一份、`scripts/serve.py` 在建 app 前
替换 `StaticFiles.file_response` 直接发那份产物(运行期零压缩开销,正好保住 Streamlit 顾虑的 CPU/RSS)。
线上字节 2.50 → 0.77 MB,模拟跨境首屏 8.6 → 6.4 s。**部署方式随之改变**:生产不再直接起 `streamlit`,改起 `serve.py`
(`deploy/novastory.service` 与 `DEPLOY.md §4.5` 已同步),升级 Streamlit 后必须 `make precompress`(忘了会自动回退发原文,
不会发错版本)。`deploy_check` 新增「静态压缩」闸门。
**同日查明:线上早有 nginx 反代 + 证书 + HTTP/2**(`https://test.jp.wuzuxi.com`,后端 etag 与本机 8501 一致),
所以既往那条「无 HTTPS 反代」红灯是**误报** —— nginx 不在本机,`deploy_check` 却在查本机 :443。已改为读
`secrets.toml` 的 `public_url` 从外面实测(新增 `check_public`)。随之 HTTP/2 那笔加速本来就已到手,
**真正缺的只有反代层 gzip**:线上主 JS 仍以 440 KB 原文下发,实测 200ms/4Mbps 链路 5.8 s,开压缩后 2.4 s。
nginx 配置与两个易漏点(`gzip_types` 默认只含 text/html、`gzip_proxied` 默认 off)写在 `DEPLOY.md §4.5`。
另:JS 合并/删 modulepreload 经三路调研**否决**(首屏只加载 104/363 个,其余是按需分包;合并会拉进从未使用的
Plotly 4.7MB 等,并打断 142 处动态 import 与 152 处 import.meta.url)。三条待拍板记在 `13 §⑦-M`。

---

## 2026-09-02 · `代码` **第四轮全方位审计:研究员入口改 `?admin=1`、总问卷事件移出轮次、功效改解析解、`make analysis` 两处必崩修复**

多专家对抗审计(建图 9 子系统 + 12 视角 finder + 复现验证)后一次性修了 63 个文件。会改变**操作或口径**的四条:
① 研究员登录框不再对被试可见,**网址加 `?admin=1`** 才出现,并按 IP 节流;② `final_survey_shown/submit` 与 `contact_submit`
一样写在 `round_idx=0`,不再落进第 3 轮的事件窗口;③ `power_sim` 的配对 t 功效改**非中心 t 解析解**(MDES 数字不再带蒙特卡洛噪声);
④ `make events` / `make embed` 单跑必崩(前者缺 `sys.path`,后者引用未定义的 `DB`)—— `make analysis` 一条龙此前根本跑不通,
smoke 以模块方式导入所以从没抓到。另:完成码幂等 + 去同形字、问卷记 `trial_id`、配图目录按 attempt、重试一次只生成一次、
被打断的 LLM 等待照记(`llm_abandoned`)、`pilot_check ④` own/soa 分开判(新常量 `SOA_R_*`)、α / `SHOT_TAGS` / 基线下限进 `prereg`。
未拍板事项与命名建议全部记在 `13 §⑦-H`。

**第二批(2026-09-03 凌晨,设计评审 87 条主张 → 23 条存活后)**:⑤ 终稿已提交、问卷未交时刷新**恢复到问卷页**而不再整轮重做
(此前 12 号文档记为「设计如此」,现改);⑥ `trials_archive` 保留被 OR REPLACE 顶掉的尝试;⑦ `config.toml` 改生产默认(不监视文件、
断线会话保留 10 分钟)、开发用 `make dev`;⑧ 口令 fail-closed(无默认口令);⑨ 每行 trial 记 `app_rev`;⑩ 轮内文案由「1〜2文」改为
「長さ・形は自由」(与说明页和 B6 一致)。被反驳员驳回、**没改**的:seq 解码次序对调、去掉 C 的 ⚠️ 加粗。待拍板项见 `13 §⑦-H`。

受影响:`app.py`、`core/{state,db,llm,shots,imagegen,llm_batch}.py`、`views/*`(含新 `_first_gen.py`)、
`analysis/{prereg,v3,events,stats,pilot_check,embed,figures,power_sim,norming}.py`、`scripts/*`、`i18n/*`(+6 键)、
`docs/{index.html,paper/03,04,07,09,README}`、`requirements*.txt`。

---

## 2026-09-02 · `决策` **同意书补齐 + 两条新拍板(年龄保持 / 邮箱走同意书)**

**① 年龄档保持 11-20 不动**(用户拍板)。三轮审计里这条被独立命中 3 次、评级 major,
仍然维持 —— 但连带后果必须留档,答辩/审查要主动交代:
`age_idx` 存的是**档位序号**,事后**分不开 11-17 与 18-20**;因此同意书里**没有**写
「18 歳以上」这类资格条款(写了就与题面自相矛盾)。「如何确保没有未成年人参加」的
诚实答案是:**没有设门槛,也无法事后识别**。

**② 完成页的自愿邮箱走「写进同意书」这条路**(§0.7 的 (a) 方案),而不是把邮箱移出被试行。
同意书三语重写,顺带把 `13 §0.1` 列的必备项一次补齐:分析阶段的**二次外发**
(`embed`/`judge` 会把被试原文再送一次给 OpenAI —— 这是此前**超出已同意范围**的一段)、
成果刊载、保存年限、参加与否**不影响成绩评价**、中止时的数据处置、联络先。
研究者信息与保存年限以 `〔…〕` 占位,新增 `deploy_check` **红灯闸门**:不填掉不许发链接。
同时把那句加粗禁令**限定到「絵コンテやアイデアの入力欄」** —— 原来它读起来是通盘禁令,
而二十分钟后我们又管人要邮箱。

**③ 顺带修掉第三处「写了但从不执行」**。同意书要写「中止した場合、そこまでのデータは
分析に使用しません」,写之前先查 —— 这句当时**是假的**:`v3.py` 把 `status` 带进管线、
注释也写着「离脱者不进分析」,但规则从来没执行过。已新增
`prereg.ANALYSIS_REQUIRES_ALL_ROUNDS` 并在 `v3.load()` 里真的执行(以**问卷提交数**为准,
不看 `status` —— 三轮都答完只差总问卷的人不该丢)。**这是继 `NOVICE_DEF`、
`DECISION_BRANCHES` 之后的第三例;下次写「规则」时先问一句:管线里哪一行会执行它。**

**④ 测量上新增三处**(均为**探索性**,要当确证必须在采数前写进 `prereg` 与 `04`):
筛查加自我效能题 `script_confidence`(前测、不进 novice 定义);
总问卷加 `closest_round` / `effort_round` 两条**跨轮强制选择**与 `noticed_diff`
**操纵察觉**开放题。跨轮强制选择的价值在于:轮内的 `imagine` 比的是**被 E 自己重塑过的
记忆**(`11` 致命级 #2),而做完三轮后是三个成品摆在一起比 —— 偏差结构不同,是收敛证据。

受影响:`i18n/*`(298 键)、`views/{screening,final_survey}.py`、`app.py`、
`analysis/{prereg,v3}.py`、`scripts/deploy_check.py`、`docs/paper/{03,13}`。

---

## 2026-09-01 夜 · `设计` **被试端 15 项交互修订(其中 3 项动到测量口径)**

一批以「被试看得懂、改得动」为目标的界面修订。**多数是纯呈现,但有三项动了口径,答辩要能说清:**

| # | 改动 | 是不是只动呈现 |
|---|---|---|
| ④ | **说明页与背景问卷对调** → 同意 → 背景问卷 → 说明页 → 第 1 轮 | ❌ 动了 onboarding 的位置与 intake 时长口径 |
| ⑧ | **C 在写创意前就明说「只生成一次、不能改也不能重生成」**;C 提交后回显被试自己写的创意;C 的提交按钮改中性文案 | ❌ **C 的操纵强度被明确化了** |
| ⑮ | 题目卡的「是看地图 / 循着气味 / 开口问路」移进**浅色括号**,并加一句「或是你自己奇思妙想的故事」 | ❌ 放松了题目导向 |
| ①②③⑤⑥⑦⑨⑪⑫⑬⑭ | 顶栏黑条、例子编号与强调色、年龄改 10 岁一段、"设计"→"平面设计"、分镜两行排版、总问卷标签去括号、完成页文案、**完成页自愿留邮箱**、重置按钮改名 | 呈现层;⑥⑨⑬ 有连带,见下 |

**三项口径改动的理由与代价:**

- **④ 对调**:说明页是被试动手前最后读到的东西,排在 12 项问卷之前会被问卷挤出记忆。
  身份与续接 token 提前到筛查提交时写(`state.enter_intro`),`round_start` 推迟到说明页
  的「开始」按钮(否则说明页停留会折进第 1 轮 `t_read_intent`);intake 总时长口径改为
  `consent_shown → intro_continue`;未答完第 1 轮的续接改落说明页。
- **⑧ C 的明确化**:原本 C 也是只生成一次,但被试**事前不知道**,提交按钮还写着「满意了」
  —— 暗示存在一个可以不满意、可以重来的版本。现在事前讲清 + 中性文案,
  C 量到的是「一次成型」本身,而不是「以为能改结果发现不能」的落差。
- **⑮ 题目松绑**:三题的选择列表原本与设定同字重,读起来像「只许这三条路」。
  被试若为迁就它而收窄自己的想法,那部分意图就是我们替他写的 —— 而保真恰恰在量这个。
  ⚠️ 代价:三题的 norming(开放度可比性,`analysis/norming.py`)是在**旧措辞**下做的,
  松绑后是否仍可比**没有重测**。

**连带改动:** ⑥ 年龄从 5 档(18-24 起)改成 6 档 10 岁一段(**11-20 起**)——
⚠️ 这与「缺 18 岁门槛」那条未决项直接冲突,见 [13 §第 0 层]。
⑨ 分镜两行排版牵动 `prompts.py`(script + revision × 三语)与 `core/shots.py` 的兜底切分
(改按「每镜首字段」切,新旧两种排版都认)。
⑬ 完成页自愿留邮箱 → `participants.contact_json`,**不进任何分析**,但与同意书「匿名」
口径冲突,采数前必须处理(见 [13 §⑦])。
⑮ `topics.json` 拆出 `choices` 字段,`prompts.scenario_text()` 拼回给模型。第 1、3 题拼回后
与改动前**逐字相同**;**第 2 题的语序变了** —— 当晚审计指出它的第二句
「ほんの数秒の攻防。/ 短短几秒的拉锯。」是**节奏提示**(15 秒 3 镜的由来)而不是选择项,
被塞进「可以不理」的浅色括号里等于把情境框架当成可选装饰,故挪回 `scenario`,
拼回顺序随之变为「设定 + 节奏 → 选择」。`deploy_check` 的题库闸门已把 `choices` 纳入三语校验,
并把「字段整个缺失」升为 🔴(没有语言可回退,该题的情境在 prompt 与题目卡上都会少半句)。

**当晚审计**(7 维并行 + 3 路对抗证伪)在这批改动里抓出 **23 条**并全部修掉,逐条见
[13 号 §⑦-B](13_待办总表_20260901.md)。其中最该记住的三条:解析器兜底的锚点从「时长」
换成「画面描写」之后,「切得多的赢」这条老启发式会被**有内容、会重复**的字段骗到
(凭空多镜 / 两镜粘一镜);`t_intake_total` 与 `t_intro` 两个时长口径在换序后都变了含义;
完成页那张表单是全应用唯一没兜异常、且能被转发网址写入的 DB 写。

**验证**:`make i18n`(290 键 × 3 语一致)· `make smoke-e2e` · `make robust`(30 项)·
`make smoke`(25 项)· 另跑 4 场完整被试流程(2 场真并发进程 + 2 场顺序,ja/zh/en 各有)
逐项体检 40 条断言全绿。

---

## 2026-09-01 晚 · `决策` **事前意图快照屏:加了又撤**

当天下午按 2.5 新增的「事前意图快照」屏(三条件同文、AI 介入前写 3 条「一定要有的东西」),
当晚经用户判断**全部撤销**并清干净(代码/DB 列/事件/分析指标/i18n/测试)。三条链复跑全绿。

**保留**:保真第三腿 `mine_ratio` → `not_against`(2.5 的另一半)、`ai_q_best` 移到逐镜头之后、
两题的**时点锚定措辞**(「はじめに」/「一开始」)。

**撤的理由**:它不产生任何进入冻结复合的数字(0%),只保护既有测量的效度;
每轮 30–60 秒 × 3 轮压在本就未实测的「10–15 分钟」上;且引导文案直接向被试报出了一个主 DV
(需求特征,正是 11 rank14 批过的「界面文案预热主 DV」)。

**⚠️ 代价**:`11` 致命级 #2「保真主终点构念自循环」**重新未缓解**。
`imagine`+`violation` 占保真复合 **1/3**,而保真的**等价检验 100% 跑在 `imagine` 上**
(`prereg.EQUIV_DV_BY_ENDPOINT`)。可选缓解与建议见 [13 号 §⑥](13_待办总表_20260901.md)。

---

## 2026-09-01 晚 · `代码` **自查复查:上午那批改动自己查出并修掉 7 个问题**

6 维并行审查 + 3 路对抗证伪 + 故障注入。**完整表见 [13 号 §⑤](13_待办总表_20260901.md)。** 三条严重的:
① **判定分支只看 p 不看方向** —— 显著性双侧,`E 显著劣于 D` 会被判「判优」,能把结论写反。
   已加 `inferior` 分支 + `estimate` 回传 + 非主终点直接 ValueError。
② **SESOI 量纲错配** —— `fidelity_composite` 是 z 合成的(实测 sd=0.62),把界写成 0.5「分」
   实为 **0.81 个标准差**。已新增 `prereg.EQUIV_DV_BY_ENDPOINT`,保真的等价检验改在原始锚题 `imagine` 上做。
③ **三分支从没被执行** —— `DECISION_BRANCHES` 只是字符串,`main()` 只对 H4 调 tost。
   这正是我批评过的「NOVICE_DEF 写了但从不执行」的同一种病。已接进 main 并加四路径断言。

另修:`t_snapshot` 时长列缺失、快照只存不用(新增 `v3.snapshot_metrics` 含 `snapshot_coverage`)、
空 novice 子集 exit 0、ceil 浮点偏 1、`bool(NaN)` 恒 True、两处文档口径过期。

**两条过程教训**:①我写的第一版量纲守门是**空断言**(z 列取均值后 sd≈0.62,`|sd-1|<0.15` 恒假被 and 短路),
靠故障注入才发现 —— 此后每个新门禁都注入一次故障确认真的会拦;
②审查 workflow 的 agent **没有 worktree 隔离**,一个 agent 在做故障注入时覆写了 `analysis/stats.py`、
抹掉方向修复,是 smoke 断言抓出来的 —— 这类审查以后要么只读、要么用 worktree 隔离。

---

## 2026-09-01 下午 · `决策`+`代码` **13 号待办总表逐条拍板 → 主分析人群/SESOI/保真构念/冻结体制全部落地**

用户对 [13_待办总表](13_待办总表_20260901.md) 逐条拍板,同日实施完毕。**完整对照表见 13 号「拍板与执行结果」一节。**

**方向性的四条**:
- **2.7 冻结时点 = 方案 A(先验锁死)** —— 主终点/embedding 去留/novice 定义/SESOI 全部在见到任何真人数据前锁死;
  试测只排雷,**数据不入分析**。`05 §1.4/§4` 已重写,原「试测后锁定 8 项」作废(理由:试测数据就是真人数据)。
- **2.4 H4 降为描述性** —— `spec_ok` 是二值且实测三条件都 100%,配对差 SD≈0。不换「有方差的代理」
  (MATTR/终稿距离量的是多样性/改动量,不是质量,改名即偷换概念)。改为如实写
  「本研究は作品の質を測定していない」。`prereg.H4_IS_CONFIRMATORY = False`。
- **2.5 保真复合去循环** —— 第三腿 `mine_ratio`(=贡献量,与 own3 同构念,使「E 保真最高」成为**定义性**结论)
  换成 `not_against`(=意图一致性);**新增「事前意图快照」屏**(三条件同文、AI 介入前),
  `q.imagine`/`q.violation` 加时点锚定并在问卷里原样显示快照;`ai_q_best` 移到逐镜头标注之后。
- **0.6 招募口径 = 無償(大学内或社会上)** —— i18n 删掉假设众包平台的完成页文案。

**代码侧**:`prereg.is_novice()` 成为唯一会执行的人群定义(此前 `NOVICE_DEF` 是从不执行的字符串);
`v3` 导出 novice/status/seq;`stats --population novice`(默认)且旧 CSV 缺列直接 exit 1;
`SESOI` → `SESOI_BY_ENDPOINT` + 三分支 `DECISION_BRANCHES`(TOST 不过 = **INCONCLUSIVE**,⛔ 不得写「不劣」);
新增 `scripts/freeze_prereg.py`(冻 prereg + 16 个管线旋钮 + 9 个源文件 sha256 + 依赖版本,`--check` 可校验);
`power_sim` 新增子集功效表;`backup_db.sh` 纳入插图 + 磁盘告警;同意书版本 sha1 落库。

**⚠️ 本轮最重要的数字**:`power_sim` 实测确认 `11` rank3 的预警 —— **novice 子集 N=18 时 MDES = dz 0.706**;
对外宣称的 dz≈0.48 是全样本数字,主分析要达到同等灵敏度需招 **77–113 人**。
**子集目标 N 是冻结的最后一块拼图**,取决于招募渠道与时间账(13 第 3 层,用户择期处理)。

**回归**:三条链(`smoke-e2e` / `smoke` / `robust`)全绿;i18n 三语 287 键一致;`deploy_check` 红灯 2 → 1
(剩余红灯 = 模型置顶,用户自己手改)。

---

## 2026-09-01 · `文档` **全库待办扫描 → [13_待办总表](13_待办总表_20260901.md)**

7 路并行扫描 `docs/paper/03–12` + `DEPLOY.md` + `deploy_check` 实跑 + `analysis/*` + 代码 + i18n,
249 条去重后按**阻塞层级**重排。**核心结论:`11` 号审计(2026-08-04)的 P0 项一个月后逐条核实几乎零进展**
——期间 6 次提交里 4 次是文档、1 次是 embed 基线校验、2 次是 09-01 的 UX/韧性工作,都在另一条轴上。

**已核实的关键事实**:`prereg.NOVICE_DEF` 是从不执行的字符串 · `v3` 不导出 novice/status/seq ·
`stats` 无 `--population` · `power_sim` 无子集功效曲线 · `SESOI` 仍是标量 · 无 `freeze_prereg.py` 且
`SPEC_TOL_SEC`(决定 H4 主 DV)在冻结范围外 · `consent.body` 缺 6 项必备内容(其中**分析阶段的二次外发
与实际行为直接冲突**) · 无 18 岁门槛 · `researcher_password` 未配 · `api_configs[0]` 仍是 edgefn ·
无 `data/baseline/` · 备份从未成功跑过 · 无 `00_工程表.md`。

---

## 2026-09-01 · `代码` **实测就绪性验证 —— 修掉「网关空返回全线静默」等 3 个现场问题**

把「多用户 / 中途刷新 / 断网 / 脏数据」在临时库上**真跑了一遍**（不是读代码），固化为
`scripts/robustness_check.py`（`make robust`，30 项）。产出 → **[12_实测就绪性验证](12_实测就绪性验证_20260901.md)**。

**修掉的 3 个真问题**：
① **网关返回空串（不抛异常）时六条生成路径全线静默** —— 被试点「告诉 AI」/「完成作答」**毫无反应**，
状态条还写着「生成完成」。`generate_json` 早已处理这种形态，streaming 路径漏了。修在
`views/_streaming.py` 单点（记 `llm_error{kind:"empty"}` + 提示 `errors.llm_empty`），六个调用点全受益，
且空返回率现在可事后统计。
② **题库写坏时闸门不拦、被试撞崩且已消耗一个 seq** —— `deploy_check` 新增题库闸门（损坏/不足 🔴、
三语不齐 🟡），`screening.py` 在 `insert_participant` **之前**加运行时校验（`errors.not_ready`），不消耗 seq。
③ 逐被试时长表落盘无视 `--csv` 污染项目树（被 `analysis_smoke` 的断言当场抓住）。

**验证结论**：24 人同一瞬间通过筛查 seq 无重复无跳号；WAL 下研究员后台读全表与被试写入不互斥
（**采数期间可随时开后台**）；各阶段刷新不产生重复被试行/不消耗第二个 seq；伪造 token 不崩不泄露；
配图 API 全挂 2.5s 内 settle。**残留 4 条风险全部有对策或已判定可接受**（12 §4）。

**受影响**：`views/_streaming.py`、`views/screening.py`、`scripts/{robustness_check(新),deploy_check}.py`、
`analysis/events.py`、`i18n/locales/*`（278 键）、`Makefile`（`make robust` / `make smoke-e2e`）。

---

## 2026-09-01 · `代码` **全流程 UX / 埋点 / 计时审查 —— intake 段补齐、量表统一、`?lang=` 参数**

一次覆盖被试全流程（同意→说明→背景问卷→3 轮→总问卷→完成码）的审查。**落地七项**：
① **`?lang=ja|zh|en` URL 参数**（`core/state._apply_url_lang`）—— 只在被试行创建前生效一次，续接被试用库里的 `lang`，未知值回落 ja；
② **intake 段埋点**（`consent_shown`…`screening_submit`，写 `round_idx=0`，筛查时由 `db.attach_intake_events` 回填 participant_id）—— 此前同意页/说明页/背景问卷零记录零计时；
③ **`participants.finished_at`** → 整场用时可算；
④ 补 `final_survey_shown` / `retry_click` / **`guidance_cancel`**（E 中途放弃引导，此前完全静默）；
⑤ **7 点量表统一到 `views/_scale.py`**（背景问卷 / 每轮问卷 / 总问卷同宽同锚点）；
⑥ **漏答提示统一为点名**（背景问卷 12 题此前只说「有漏的」）；
⑦ `analysis/events.py` 新增 `per_participant()` → `v3_per_participant.csv`（intake 三段 / 总问卷 / 整场用时），逐 trial 表排除 `round_idx=0`。

**受影响**：`core/{state,db}.py`、`views/{_scale(新),consent,intro,screening,questionnaire,final_survey,guidance,group_c,group_d}.py`、`i18n/locales/*`（276 键，三语一致）、`analysis/events.py`、两个 smoke。
**新的待拍板 ⑪–⑯ → [06_待拍板事项](06_待拍板事项.md) 首节**（同意书「10-15分」是否属实 · 要不要「退出」按钮 · 「刷新会续接」在 intake 阶段不成立 · 总问卷轮次指代 · 进度条覆盖 · 问卷逐题埋点）。数据字典同步 → [03 §6/§7/§8](03_测量与数据字典.md)。

---

## 2026-08-04 · `文档` **全文书审计 → [11_文书审计与新TODO](11_文书审计与新TODO_20260804.md)**

**2026-08-04 `文档` 全文书审计**：对 `paper/`(归档)+`docs/paper/01–10`+`docs/index.html`+`README`+`CLAUDE.md`+`DEPLOY.md`+i18n+代码做了一次系统审计。产出 → **[11_文书审计与新TODO_20260804](11_文书审计与新TODO_20260804.md)**（30 条矛盾 · 统一术语表 · 六视角批判 30 条 · 新 TODO）。同日已应用机械性修正（失效指针 `paper/13|16`→`docs/paper/07|05`、`CLAUDE.md` 三处陈旧常量、`09` 的 E/D 通道非对称与 H5 定位、i18n 里的禁用措辞、`02` 逐镜头标注口径、`03` 列名别名、`prereg.py` 的 H4 DV 注释、`reference/06` 的 A1 rubric 已弃）。文献侧：新建 `reference/07_统计方法与设计.md`，补入 121 条并**更正近邻四篇**（Maier 实为五臂 d=0.57 / IntentFlow = DIS 2026 / Timing Matters 补 DOI / Tix 的 HICSS 2025 venue 存疑）。**未拍板项集中在 11 §6**（伦理审查依据、保真复合冻结形、冻结时点、novice 定义与目标 N、H3b 非劣、H6–H8 去留、灵魂句断定形、修士论文降级预案）。

---

## 2026-08-03 · `决策` **【既定】配图模型 = `gpt-image-1-mini`** · 语言进分析管线 · Embedding 缺口定性

### ✅ 既定事项：配图模型锁定 `gpt-image-1-mini`（`core/imagegen.py:_IMG_MODEL`）

**全 6 个图像模型实测**（3 张并行 × 2 轮，同一 `_STYLE` 风格锁，压到与线上一致的 512×512 JPEG）：

| 模型 | 3张并行 | 出力tok | $/张 | 全研究(405张) |
|---|---|---|---|---|
| gpt-image-1（原用） | 17.9s | 272 | $0.0109 | $4.4 |
| **gpt-image-1-mini ← 采用** | **13.7s** | 272 | **$0.0022** | **$0.9** |
| gpt-image-1.5 | 9.9s | 410 | $0.0131 | $5.3 |
| gpt-image-2 / gpt-image-2-2026-04-21 | 22.8 / 25.8s | 196 | $0.0059 | $2.4 |
| chatgpt-image-latest | 13.3s | 555 | 价格未公布 | — |

**决策依据（四条，按权重）**：
1. **最便宜且差距决定性** —— $0.0022/张，比第二便宜的 gpt-image-2 还便宜 **2.7 倍**、比 gpt-image-1.5 便宜 **6 倍**。（gpt-image-2 出力 token 更少 196 vs 272，但单价 $30/1M vs $8/1M，算下来仍贵 2.7 倍。）
2. **零安全拒绝** —— OpenAI 图像安全系统会偶发 400 拒绝**即便提示词完全无害**。实测 42 次：mini **42/42（0 拒绝，全部 60+ 次尝试无一例）**，gpt-image-2 **39/42（7%，每一批都出现）**。按 405 张算 7% ≈ **28 格缺图且分布不均**。
3. **线条最干净** —— 彩度 **0.0**（原 gpt-image-1 有 1.1 轻微串色，而风格锁明写 "No color"）、连通成分 5 / 微小碎片 0（原 10 / 4）。gpt-image-1.5 与 gpt-image-2 笔画严重碎裂（成分 86 / 264）并出现阴影块，违反 "clean thin uniform black outlines / NO shading"。
4. **速度不是区分点** —— 最快的 gpt-image-1.5（9.9s）只比 mini（13.7s）快 3.8 秒，却贵 6 倍。配图在**问卷阶段后台生成**，问卷本身 60-90 秒，这点差别被试感知不到。

**参数已在最省档，不要再"优化"**（已钉成 `core/imagegen.py` 调用点的注释）：
`256x256`/`512x512` → **400 Invalid size（不支持）**，1024×1024 是最小可选；`size="auto"` 会选 1536×1024 **贵 47%**，必须写死；`quality` low=272 tok，medium/high·auto 分别贵 **3.9× / 15×**。
→ 「生成 1024 再缩到 512 存盘」**不是浪费**，1024 就是生成下限。

⚠️ **图像模型无日期快照可钉** —— 账号里唯一带日期的是 `gpt-image-2-2026-04-21`，**没有选它**：钉快照是为了「别让 DV 里的视觉成分中途变化」，而它 7% 的拒绝率会**当场**制造 ~28 格缺图 —— 用一个**确定的、现在就发生的**不一致，换一个**假设的、将来才可能发生的**漂移，不划算。
→ 这是 **B7「钉日期快照」规则的已知例外**。缓解：① 配图不是 DV；② 被试内在 C/D/E 均等，漂移**不偏 E−D 主对比**；③ 每张图归档 `data/storyboard_images/`，可事后复算风格指标检测漂移。**Limitations 写明。**
对照图 `samples/imggen/model_bench_2026-08-03.jpg`，完整数据 `samples/imggen/README.md`。
- **被试语言进分析管线**：`participants.lang` 一直有记录，但 `v3.load` 只取了 `id, screening_json` → **语言从未进过分析数据**。现已 merge 进每行 trial、`per_trial` 输出 `lang` 列、`v3.py` 打印语言构成并在非 ja 时告警；监控台概览也显示（非纯 ja 变黄）。理由：prompt / 题目 / 解析器 / 量表 / embedding **全随语言变**，分析必须能按它切分，论文要写明语言构成。
- **Embedding 缺口定性**（[03 §4.1](03_测量与数据字典.md)）：保真 Δ 要「≥3 个异构日语模型」，但 OpenAI 只有同族的 small/large/ada —— **取均值等于同一观点说三遍**，一致性带虚高。**必须补一个开源模型**（multilingual-e5 / bge-m3，本地跑，顺带避免被试文本二次出境）。保真 Δ 是采数后才算的，**不挡采数**，但分析前必须做完。
- 影响 `core/imagegen.py`、`analysis/v3.py`、`views/monitor_panel.py`、`i18n/*`、`samples/imggen/README.md`、`docs/paper/{02,03,06,07,10}`。

## 2026-08-03 · `决策` 模型选型：测试期 4o-mini，正式采数换 gpt-5.4-mini

- **方法**：拿本项目真实 prompt（日语、真三题）实测 9 个候选，不查参数表。延迟与成本**都不是区分点**（全在 8s 内；全研究文本成本 $0.11–$3.39，怎么选都不破预算；gpt-5.4 系 `reasoning_tokens=0`）。
- **区分点 = E 引导问题的具体性**。E 的问题**本身就是干预**，问题空泛 = 操纵变弱 → 这是处理效度问题。gpt-5.4-mini 会把创意里的**具体两难**摆成互斥选项；gpt-4o-mini 给的是抽象名词三选一。
- **稳定性实测（各 20 次）**：引导 JSON 首次解析 40/40 成功 → **fallback 实际不会发生**（回答了长期挂着的「引导约束只靠 prompt、待实测」）；`parse_ok=0` 未出现。**唯一真实差距**：gpt-4o-mini ~5% 概率产出违反「必 3-4 选项」硬底的 2 选项题或问数越界，且代码不做实时校验；gpt-5.4-mini 0/20。
- **B4 提示词校验**：两个模型全路径 × 真三题 × ja **都通过**。顺带更正一版误报——GPT-5 系的「秒漏」是正则把「15秒の中で」这种背景说法当成了违规，实际 0 违规。
- **决定**：**现在（开发/试测）继续用 `gpt-4o-mini-2024-07-18`；正式采数前换 `gpt-5.4-mini-2026-03-17`。** `secrets.toml` 已备好该配置块（移到第一位即可），`deploy_check.FORMAL_MODEL` 会在模型不是它时亮 🟡。
- **顺带发现的两个缺口**：① 配图 `gpt-image-1` **无日期快照**（违反 B7 钉快照规则）→ **已由上方同日「配图模型 = gpt-image-1-mini」条取代**（结论：不换 gpt-image-2，理由见该条）；② 保真 Δ 要「≥3 个异构日语 embedding」，但 OpenAI 只有 2 个 → **必须补一个非 OpenAI 的**（仍未做）。
- 影响 `.streamlit/secrets.toml(.example)`、`scripts/deploy_check.py`、`docs/paper/{02,06,07,10}`。

## 2026-08-03 · `决策``代码` 采数前拍板 B1/B4/B6/B7

- **B1 novice 定位**：**招募端不设门槛（随机找人）**，但 `is_novice` 子集 = **事前确定的主分析人群**；全样本作稳健性；有创作经验者单列做 **H8「経験あり vs なし」对比/调节分析**。连带：功效按子集算、超招募到子集达标为止。影响 `analysis/prereg.py`、`docs/index.html` 手法页、`docs/paper/{02,04,05,06}`。
- **B4 伦理审查**：**不提交伦理审查**；但同意书**必须**明示被试文本会发送到 **OpenAI（米国）** 及保留/训练政策 → 三语 `consent.body` 已实装（APPI 越境粒度）。影响 `i18n/locales/{ja,zh,en}.json`、`docs/paper/{06,07}`。
- **B6 意图字数下限**：`MIN_INTENT_CHARS` **10 → 8**，对齐说明页「只给基调也行」的文案。影响 `core/config.py`。
- **B7 可复现性**：**实测 gpt-4o-mini：temperature=0 + 固定 seed 下 5/5 次输出仍不同**（`system_fingerprint` 相同）→ OpenAI 的 seed 只是 best-effort，批式推理非 bit-reproducible。**目标改为「配置可复现 + 漂移可检测」**：① 模型钉死日期快照 `gpt-4o-mini-2024-07-18`；② 每次调用发 per-trial 派生 seed `(pid×100+round)`（不用全局常量，避免压缩被试间方差污染 H6）；③ seed + `system_fingerprint` 落 `llm_done.payload.repro`；④ temperature 维持 0.8。影响 `core/llm.py`、`views/_streaming.py`、`.streamlit/secrets.toml(.example)`。E2E 全绿 + 真机验证。
- **B2 SESOI**：锁定 **`prereg.SESOI = 0.10`**（DV 原始单位；结构完整度 0-1 比例 → 10 个百分点）。0.05 过窄会自断非劣性主张、0.15 过宽；a priori 设定（N=0）。`stats.tost` 实测：差 3 点判等价、差 20 点判不等价。影响 `analysis/prereg.py`。
- **B3 预注册**：**不做第三方预注册**（OSF/AsPredicted）。改为**内部冻结**（`prereg.py` + 决策树 + 假设表 → `as_dict()` dump 带 hash + git commit）。⛔ 连带硬约束：论文/发表**不得写 "preregistered" / 「事前登録」**，Limitations 加一条，答辩坦承弱于预注册。`docs/index.html` 的「事前登録」措辞已全部改掉。影响 `analysis/prereg.py`、`docs/index.html`、`docs/paper/{04,05,06,08}`（05 更名为「分析计划冻结与试测决策树」）。
- **B5 A1 质量 rubric**（用户授权我定）：**四维审美 rubric 弃**（TTCW：LLM 评创意与专家相关≈0；质量已由结构完整度+TOST 覆盖）；**`scripts/judge.py` 的盲评保真保留**作次要/收敛证据（属结构化比对 = Zheng 2023 适用域，且已实装、已日语化、OpenAI×3+ICC，边际成本≈0）。顺带更正：旧文档称 judge.py「提示是中文/仍写异家族协议」——读文件后确认**已不成立**。影响 `scripts/judge.py` 横幅、`docs/paper/{03,04,06}`。
- **仍未做的连带动作**：`power_sim` 按 novice 子集 + SESOI 重算功效曲线；B4 引导质量在新快照上复测。

## 2026-08-03 · `文档` paper/ → docs/paper/ 迁移

- 旧 `paper/` 整体标为过时（仅历史留档）；仍有效内容全部迁入 `docs/paper/`（本目录）。
- 迁移中修正的三处不一致：① paper/10·15 的「3×3 拉丁方」→ **Williams 18 系列**；② 保真三角第三腿「盲评保真」→ **embedding Δ**；③ 日语「引導」→「意図の引き出し」。
- 新发现的未决冲突（**novice = 主分析人群 vs 协变量**）与全部开放项汇总到 [06_待拍板事项](06_待拍板事项.md)。

## 2026-07-31 · `决策` ODAF2026 展示用タイトル確定 + 日语用語ルール

- 展示用（E のみ、対照は書かない）：「**意図を引き出してから生成する：非専門家の物語創作を支える意図引き出し型の創作支援システム**」。**論文タイトルは別建て**（対照を戻す）。
- 旧タイトル（「表現の均一化と最適な人的介入プロセス」）**廃止**：均一化は H6 探索に降格、「最適な介入プロセス」は 3 条件では言えない、「生成AIエージェント」は実装と不一致。
- **用語**：日本語で「**引導**」は使わない（仏教語・「引導を渡す」の含意）→「**意図の引き出し**」に統一。

## 2026-07-19 · `设计` 拉丁方 → Williams 全 6 排列（**最重要的设计修正**）

- 旧的 3 循环序列（CDE/DEC/ECD）单向转圈 → **D 永远紧跟 C、E 永远紧跟 D**，把招牌对比 **E−D 与一阶顺序残留混淆**。
- 改为 **C/D/E 全 6 排列（Williams）× 3 题目轮换 = 18 系列**，`config.LATIN_SQUARE_N=18`；实测 6 个有序相邻对各 6 次、条件×位置/条件×题目均配平。
- 同批：**停止规则**改为「招到各 seq 完成数达标为止」（超招募吸收流失），监控台改看各 seq 完成数。
- 影响：`core/{config,state,db}`、`docs/index.html`、全部方法学描述。

## 2026-07-19 · `代码` 正式轮分镜配图（仅 OpenAI 时开启）

- 提交**之后**、问卷阶段后台并行生成 3 张简笔插图（当时用 gpt-image-1；**2026-08-03 已换 `gpt-image-1-mini`**，见文首条），**不进创作净时长**；非阻塞、失败静默降回占位。
- ⚠️ **遗留效度注记**：配图在问卷阶段可见 → 保真/所有权 DV **含视觉成分**。被试内在 C/D/E 均等（不偏 E−D），但改变构念解释，是知情决定 → 必须写进 Limitations。

## 2026-07-19 · `代码` 同意后加「はじめる前に」说明页

- 新手对着空白输入框难下手 → **不在输入框塞模板**（会污染保真基线；若加结构化提示 = 泄漏 E），改为**同意后加一页统一说明**（标准化暴露、条件/内容中立）：流程小图 + 意图自由度文案 + 中立例题「寝坊した朝」配 5 档参考输入 + 成品分镜样例。
- 例题**刻意用非实验题**，只教「写多少/什么形式」，不锚定真题内容。

## 2026-07-18 · `设计` 移除 E 引导的 pacing（秒数分配）补充维

- AI **不问被试「15 秒怎么切给三镜」**：生成 prompt 本就让 AI 自行分配时长，再回头问新手是冗余且答不出，只膨胀提问数。
- 三支柱（psychology / turning_point / key_shot）不变；补充维改由 AI 自由补（tone/ending/sound/detail）。
- prompt 三语显式**禁止就时长/秒数提问**（round1 + followup 两处）。

## 2026-07-18 · `决策` 质量评价锁定「无人评」

- 评估建议补极小样本人评做保真效度锚，**用户决定不做**——招人评审（即便众包）有找不齐/评分漂/拖进度的不稳定因素。与 2026-07-02 拍板④一致，此为再确认。
- **锁定口径**：质量 = 结构完整度 + TOST 非劣；LLM-judge（OpenAI×3 + 自一致性）= 收敛证据/次要；**主保真 = 被试自评（想象匹配 + 逐镜头）+ embedding 相对基线 Δ**；Limitations 诚实写「无外部真人保真锚」。

## 2026-07-18 · `设计` C 条件维持「一次成型」（讨论后不改）

- 讨论过改为「多次生成」或「pick-from-3」以增强与 E 的可比性 → **维持不变**。
- 理由：多次生成 ≈ 退化版 D；pick-from-3 = 生成后识别偏好，与 E 是对立两端；加第 4 条件破坏平衡设计；**pick-from-3 会抬高 C 的所有权 → 压缩 E−C 差 → 反伤主出圈点**。
- C 的价值 = **零介入地板锚点**；「赢 C 太易」的正解是**贡献压 E vs D**。

## 2026-07-18 · `设计` 三主题重设计 + 机器基线 norming

- 旧三题（満員電車/バイト/合格発表）基调高度同质、开放度未校准、转折维贫瘠 → **系统性压低三个核心 DV 的可测方差**。
- 新三题基调正交：拾った切符 / 最後のひと口 / はじめての街の、最初の一歩。
- norming（gpt-4o-mini ×12/题）：规格 100% 合规、gzip_cr 极差 ~1% → **开放度高度可比，题面冻结**。

## 2026-07-18 · `设计` 量表定稿：soa 换 J-SoAS，own3 保留并标注

- soa1-2 换成**已日语验证的 J-SoAS SoPA** 情境化改编（Xu 2024，[A] 逐字核实），三语同步。
- own3 = **Pierce 自我投入（self-investment）facet**，**保留不改措辞**，论文标注并分报 possession / self-investment。

## 2026-07-18 · `代码` A6 分析管线 v3 全部构建 + 网页化

- 重写 `analysis/`：`textstats / v3 / norming / power_sim / stats / embed / figures`（取代 v2 HLZ 时代的 metrics/stats/power）。
- 同批搬进研究员后台（`views/analysis_panel.py`）：试测体检 / 出结果 / 出图 / 功效，**零命令行**。
- 判定：**真数据一到即可出全套结果 + 图**。

## 2026-07-18 · `决策` 修士充分性多专家评估 = **勉强够（conditional pass）**

- 纸面设计超合格线、新颖性收窄合取联网复核成立，但「够」押在**未采数据**上（N=0）。
- 答辩火力点：无真人评审锚 / D-vs-E 工作流级混淆 / 新手不设门槛 / 意图基准 1-2 句天花板 → 答法见 [08 §11](08_论文写作与投稿.md)。
- 同批：**IUI 2027 放弃**（截稿 2026-08-20 数学上不可能）→ **首投 C&C 2027 / CHI**。

## 2026-07-03 · `设计` 目标样本 N 30 → 36

- 原 30 不整除序列数会让各序列人数不均；36 在 IUI/C&C 属常见量级（C&C 2024 先例 N=36）。
- （当时按 9 序列 × 4 人算；7-19 改 Williams 后为 **18 系列 × 2 人**，N 不变。）

## 2026-07-02 · `决策` 六项一次性拍板（决策批①②）

| # | 决策 |
|---|---|
| ① | **不完整数据弃用**（未走到 trial_submit / final_survey 的 in_progress 被试不进任何分析） |
| ② | **A4 主要终点候选 + 锁定规则**：保真复合 / 所有权复合两个主终点；embedding 与 own-vs-soa 的去留由 **pilot 决定**，冻结后不可再换 |
| ③ | **评估无外部真人**（硬约束）：保真三角的外部腿全改纯算法 |
| ④ | **被试报酬 = 无偿志愿者** |
| ⑤ | **LLM-judge 全用 OpenAI**（单家族 ×3 + 自一致性，放弃异家族，Limitations 明示） |
| ⑥ | **N4 时间口径维持现状**：E 追问答题归事后侧（与 D 反馈打字时间对称）；`t_read_intent`/`t_pregen`/`t_postgen` 为净人时、`t_llm_wait` 单列、`t_total` 为毛时长 |
| ⑦ | **分析层全延后、数据侧优先**（后于 7-18 的 A6 落地而解除） |
| ⑧ | **伦理审查暂缓**（用户自定时点） |

## 2026-07-02 · `代码` 日志精简批次（数据侧收尾）

- events.ts 升**毫秒** + `seq_in_round` 轮内序号；`attempt` 会话段标记 + `trial_submit` 后 `trial_id` 回填（重做轮不再混段）；新增 `guidance_answer_saved`（E 逐题作答时刻）。
- 放弃：LOG1 每事件存内容（与 trials 全文重复）、LOG2 统一入口重构、LOG5（全版本已存可后验 diff）。
- 同批：token 用量落库（`llm_done.payload.usage`）；**LLM 失败等待不再虚增创作净时长**。

## 2026-07-02 · `设计` JSON 化剧本 —— **不做**（讨论结论）

- 剧本是被试要「看着流式生成 + 直接手改」的文档，改 JSON 会废掉流式直出和文本编辑，且 JSON 转义对长台词更易翻车。
- 结论：对人用 markdown、解析器兜底；真要保证逐镜头可后期加「提交后结构化抽取」旁路（暂不做）。

## 2026-06-26 · `代码``决策` 会话恢复 + 总问卷 + 语言流 + 招募/模型拍板

- **URL token 续接**（方案 A）：刷新/断网从 DB 重建身份与序号、跳回第一个没答完问卷的轮次；`trials`/`questionnaires` 加唯一索引 + `INSERT OR REPLACE`。（选 URL 而非 cookie：cookie 需第三方组件 + 异步首跑陷阱。）
- **总问卷 final_survey**（最喜欢哪轮 / 以后用哪轮 / 总体满意度 / 自由留言）+ **每轮满意度题**。
- **语言锁定**：语言器移到 consent 页（被试开头选一次）+ 管理员区；被试进入后不再见语言器。
- **拍板**：正式实验全程 **OpenAI**（开发期用免费网关）；招募 = **日本大学内大学生**；量表保留 **7 点**（源量表原生）。

## 2026-06-26 · `设计` 开局问卷增 3 项基线协变量

- `aiexp_idx`（AI 创作经验，区别于泛用使用频率）/ `trust`（AI 信任倾向）/ `own_trait`（心理所有权倾向），**均前测**防任务污染。
- 用途：H1-H3 协变量、H7 反向分支异质性、把「天生易有归属感」与「E 提升的状态所有权」分离。
- 控制需求（H3 trait）经权衡**未加**（soa 已测状态 agency；题量取舍）。
- **满意度/整体偏好属结果测量，不进开局问卷**。

## 2026-06-13 · `方向` 确立「事前引导 vs 事后修改」对仗（**当前主线的起点**）

- 灵魂句定调：**好的 AI 共创应在生成前规避问题，而非生成后反复修改**。
- **D = 先生成再修改（= 常规 ChatGPT 工作流代理）**，**E = 先引导再生成**；框架 = 系统评估（工作流包对比），放弃单变量。
- 核心主张经**六路对抗文献核查**：宽泛「无人做过」被推翻（APE / Maier / Timing Matters），**收窄合取仍成立**（新手 × 完整创作物 × 选项式事前引导 × 自由反馈事后 × 保真+所有权+修改量三 DV）。

## 2026-06-13 · `方向` 从 ModeMirror 转向引导式共创

- 否定 ModeMirror 的「嫌你普通、推你求异」哲学，改为**帮新手忠实实现意图（哪怕大众化）**。
- 主指标从「多样性/逃离默认（HLZ）」转向 **意图保真 + 所有权 + 修改效率**；**ghost-run 机器对照废弃**（C/D 即对照）；意图基准 = 创意 1-2 句。
- 筛查从「门槛淘汰」改为「测量记录」（谁都能做）；时长压到 10-15 分钟。

## 2026-06-13 · `代码` v3 重构落地

- C 一次成型 / D 生成后修改（脚本常驻可编辑 + AI 聊天框）/ E 引导问答（固定 3 维 + AI 补 2-4 维；选项 + 自己写 + AI 帮我决定）；**砍掉大纲层与 ModeMirror**。
- 新数据列：`guidance_json` / `revision_requests` / `script_versions` / 行为计数 / `t_pregen` / `t_postgen`；**快照硬规则**（提交/交 AI 前自动落用户手改版）。
- E 允许手改的可行性论证（双差异是已接受代价）见 [02 §1](02_实验设计.md)。

## 2026-06-12 · `方向` 二次定调：协作机制（放弃微调）

- 两方向分析后放弃「微调脚本模型」（数据/评估/新颖性三重瓶颈），选**人机协作机制**。
- 当时方案 = ModeMirror（v2），**次日即被引导式共创取代**。四路对抗验证把四个创新点全部收窄为可防御措辞——这套「杀稿式核查」的方法一直沿用至今。

## 更早（已完全作废，仅记方向）

- **2026-06-12 v1**：《控制权博弈：Agent 赋能 × 创意同质化困境 × 最佳干预点》，四组 A(纯手写)/B/C/D，主指标 = 输出多样性/意图偏离度。整体过时。
- **2026-05-26**：v1 应用（Streamlit 四组向导，CSV 存储，被试自选流程）。已废弃。
- **2026-05-21**：立项。

---

## 附：早期已结清的设计问答（一句话留档）

| 项 | 结论 |
|---|---|
| ghost-run 机器对照 | **废弃**（C/D 即对照） |
| 意图基准 | 创意 1-2 句即基准 |
| 看稿 loop 结构 | [提交 / 继续引导 / 手改]，**无上限** |
| D 的角色 | 核心对仗：D = 先生成再改，E = 先引导再生成 |
| 大纲层 | **砍掉**，D/E 直接出完整稿 |
| D 修改形式 | 自由文字框 + 允许手改**双通道**；AI 永远基于用户最新稿修订 |
| E 看稿后 | 三选：提交 / 继续引导 / 手动修改；**无自由反馈通道** |
| 第 1 轮补充维度 | 固定 3 维后 AI 补 **2-4** 个（总 5-7 问） |
| 追问轮 | 每轮 AI 出 **1-3** 问 |
| 手改交互形态 | 脚本编辑框**常驻**，无需手动保存——提交/AI 动作时自动快照 `author=user_edit` |
| 量表点数 | 7 点（源量表原生） |
| 日语字数门槛 JP5 | 不关注（当时保持 `MIN_INTENT_CHARS=10`；**后由 B6, 2026-08-03 改为 8**） |
| 引导步模型落库 | 决策消解（引导步与主模型同为 OpenAI） |
