# 待讨论事项 TODO

> 建立 2026-06-12;2026-06-26 大清理 + 改写成"人话"(技术项先一句大白话,再附代码细节)。
> **开放项在上、已结清沉到文末「✅ 已结清(留痕)」。**
> 权威设计=paper/7 v4.1;论文骨架=paper/10;主张=paper/9;文献=paper/reference/。

---

## 🧭 速览:现在该定什么、还差什么(2026-06-26 晚)

**一句话状态:** 系统主流程能从头跑到尾,刷新/断网不丢数据,语言被试开头只选一次,三轮做完有总问卷。**正式模型(OpenAI)和招募对象(日本大学生)都已拍板**——pilot 前的大决定基本清空。

**还需要你定的(不多了):**
1. **token(=花了多少钱)现在就开始记吗?** 现在没记,被试做完就查不回来了,改动很小。→ 见"补不回来"
2. **论文主分析以哪几个指标为准?** 预注册前钉死,防"挑数据"。→ 见 A4
3. (执行层)伦理审查走流程 + 预注册时点。→ 见 pilot

**本轮已拍板:** 正式模型 = OpenAI(开发期继续用免费 edgefn);招募 = 日本大学内大学生;语言 = consent 前选一次、之后锁定(只有管理员能改)。

**可慢慢来:** 数据洁净/边角小问题,见 §🟡。

---

## 🔴 跑被试前值得处理

### 要你拍板
- **token 要不要现在就记**(同"速览 1",补不回来里最便宜的一项,~10 行)。

### 补不回来(被试做完即永久丢失,跑之前最好先埋好)
- **token 用量**——大白话:每次叫 AI 干活都花钱(按字数算),现在**一个数都没记**。被试做完,这批"花了多少"就再也查不到了。
  - 细节:`generate_json` 其实已拿到用量却扔了;`generate_stream` 没开统计开关。→ 读出来记进 `llm_done` 事件(`{prompt,completion,total_tokens}`)。注:个别网关流式不返用量,取不到记空别崩。
- **操作日志记得太粗**——大白话:① 时间只精确到"秒",同一秒点了好几下分不清先后;② 没存被试**具体选了哪个、写了什么字**(只存了"写了几个字");被试中途退出,这些就丢了。
  - 细节:`events.ts` 升毫秒;`guidance_answer` 存所选文本、`revision_request` 存原文;加 `trial_id` 关联(LOG1-4)。
  - (注:整套详细日志规划见 §📋,系统冻结后一次性加;这里只是提醒 token 那条建议提前。)

## 🟡 可延后(不影响被试跑通,数据洁净/边角)

- **AUD9 引导步模型没落库**——大白话:E 的"AI 提问"那步可以配不同模型,现在不记是哪个。**已基本消解**:正式实验引导步和主模型都用同一个 OpenAI,所以不会出现"对不上"。真想留审计痕迹可低优先补记。
- **CG1 抢序号**——大白话:两个被试**几乎同一瞬间**通过筛查,可能抢到同一个"拉丁方序号",让条件顺序略微不平衡。被试少、几乎不会撞,可暂留。(细节:`COUNT(*)%9` 与 INSERT 非原子 → `BEGIN IMMEDIATE`。)
- **CG3 脏主题数据会崩**——`topics.json` 混进格式不对的旧数据会让加载报错。→ `int()` 加兜底。
- **研究员"切条件"按钮会把用时记成空**——**只有研究员测试时**用切换按钮,会让那条记录用时变空(被试正常流程不受影响)。→ 切换后补记开始时间。
- **AUD10 等待只有转圈、没进度条**——AI 生成等几十秒,只有转圈+文案,没进度条/倒计时。→ 加动态进度(关联延迟)。
- **AUD-low/nit(约 40 条,择机随手)**:注意力检查只存对/错没存原始作答;DB 连接用完没显式关;JSON 模式没用官方"只输出 JSON"开关;逐镜头三标签语义略重叠;"15秒・3カット"硬/软约束没向被试讲清;历史面板默认展开占首屏;进度条在 final_survey 阶段不显示(标题已写"最後に",可接受);consent+管理员两个语言器若同时渲染显示可能不一致(仅管理员、点一下就好)。**死代码**(仅研究员离线脚本):`ghost_run.py` 调不存在的函数会报错、`baseline_gen.py`/`ghost_run.py` 调函数没传语言、`shots.py` 的 `shot_type` 死字段、`trials` 表一堆 v2 旧列恒空。**完整清单见**(`tasks/wxqj7vwwt.output`)。

## 🌏 日语/跨语言效度(pilot/预注册前)

- **JP1 量表日译效度**:所有权(英文母本)与 agency(自译删减)的日译版效度未知。→ 用 J-SoAS 已发表日语原句;所有权回译+认知访谈;pilot 报日语 Cronbach α;论文区分"已验证量表"vs"自译版"。
- **JP2 日语 embedding 保真效度**:embedding 余弦在日语短文本上可比性存疑。→ pilot 验收敛效度;主锚用盲评+逐镜头,embedding 降辅助。
- **JP3 LLM-judge 日语可靠性**:效度证据多基于英文。→ 人评校验用**日语母语评审**,单独报日语 judge-人评相关。
- **JP4 文化反应偏差**:日本被试爱选中点/谦逊,压缩量表方差。→ 主分析用被试内差分;查条件×反应风格交互;Limitations 写明。
- ~~**JP5 字数门槛**~~ → **用户决定不关注**(`MIN_INTENT_CHARS=10` 保持现状)。
- ~~**JP6 锁语言**~~ → **已实现**(见文末已结清):consent 前选一次 + 之后只有管理员能切。
- ~~**JP7 日本招募**~~ → **已定:日本大学内大学生**(见 B8)。

## 📋 详细操作日志 — 整套留到系统定型后一次性加(现在不做)

> 目标:每个被试每个操作可逐条复盘——**时间 + 动作 + 具体选了/写了什么 + 脚本改没改 + token**。地基(`events` 表 + `log_event`)已在。
> LOG1 每个事件存"具体内容"(选项原值+输入全文);LOG2 收口到 `log_action` 统一入口;LOG3 时间升毫秒/加轮内序号;LOG4 加 `trial_id` 关联;LOG5 显式标"脚本改没改"(全版本已存,可后验 diff);**LOG6 token(=上面"补不回来"那条,唯一建议提前)**。
> 时机:系统冻结后一次迁移完成。

## 🟢 重写 paper/4 时(文档层)

- **B6 引导维度内容效度**:三维度(心理/转折/画面)需教材/从业者背书(reference/06);防"实验者自由度"。
- **B7 解释矩阵加反向分支**:Knijnenburg 2011 + Slovic 1995 →"新手嫌引导麻烦/满意度与所有权分离"。
- **效率口径三并报**:事前投入 + 事后修改量 + 总投入;轮数口径差异声明(E 一轮≠D 一轮)。
- **A3 同质化叙事位置**:降为探索性。
- 贡献声明用 paper/9 §3;Related Work 按 reference/01 切割;防御段用 reference/05。
- 顶部数据规格按已修代码更新:`r_llm_wait_pre`、`ai_decided` 布尔、人口学存 index、开局新增 `aiexp_idx`/`trust`/`own_trait`、每轮 `satisfaction`、`token` 续接列、`final_survey_json` 总问卷。

## 🔵 pilot 前/中(实验执行层)

- **B3 pilot 监控**:轮数/净用时分布;E 第 1 轮 AI 代答率 >50% 预警(补充维上限 4→2);D 的 AI 通道使用率;引导步实测延迟。
- ~~**B8 被试渠道**~~ → **已定:日本大学内大学生**(大学池招募);报酬/可达量按大学池估,pilot 实测。
- ~~**B9 正式模型**~~ → **已定:正式实验全程 OpenAI**(质量稳/延迟低,30 人 <$5);开发期用免费 edgefn。
- 伦理审查流程确认(研究科);预注册时点 = pilot 后、收数前(OSF)。**← 仍待办。**

## ⚪ 分析层(拿到数据后)

- **A1 质量 rubric 终稿**:四维已定(景别/节奏转折/视听语言/情绪锚点);待定每维锚点、judge 模型、人评份数(建议 30)。
- **A4 主要终点确切组合(同"速览 2"):** 预注册前钉死"保真复合"(想象匹配+违背反向+逐镜头 mine 比例+盲评)与"所有权复合"(own1-3 均分)。
- **A5 D 反馈编码 codebook**:`revision_requests` 映射到引导维度(图4);两名编码者+一致性。
- **A3 执行**:探索性同质化(机器地板=baseline_gen)。
- **C6 token 统计** = LOG6(建议提前,见上)。

---

## ✅ 已结清(留痕)

### 2026-06-26(晚)· final_survey 总问卷 + JP6 语言流 + 决策落定
- ~~未来"总问卷"~~ → **已实现**:三轮做完 → 新 `final_survey` 阶段(最喜欢哪轮 / 以后用哪轮 / 总体满意度 7点 / 自由留言),存 `participants.final_survey_json`;选项用「轮次号+题目标题」作记忆锚,分析端按 `trials` 把轮次映射到 C/D/E。新增阶段进 state 机 + 路由 + 续接(刷新回 final_survey)+ E2E。
- ~~JP6 锁语言~~ → **已实现**:语言器移到 **consent 页**(被试开头选一次)+ 移进**管理员区**(管理员随时可切);被试进入后不再看到语言器。技术上换成不占 `lang` 控件键 + `on_change`(新 `views/_lang.py`),避开"控件卸载清空 lang"和"format_func 崩测试"两个坑。
- ~~B9 正式模型~~ → **拍板:正式实验全程 OpenAI**;开发期继续用免费 edgefn(KAT)。
- ~~B8 / JP7 招募~~ → **拍板:日本大学内大学生为对象**。
- ~~AUD9 引导步模型不落库~~ → **决策消解**(引导步与主模型同为 OpenAI,不会对不上;降为可延后审计项)。
- ~~JP5 日语字数门槛~~ → **用户决定不关注**(保持 `MIN_INTENT_CHARS=10`)。

### 2026-06-26(下午第三批)· AUD6 会话恢复 + 满意度题
- ~~AUD6 刷新/断网即丢本轮 + 重复占 seq~~ → **方案 A**:随机 token 进网址 `?t=`,刷新后从 DB 重建身份/序号、跳回第一个没答完问卷的轮次;`trials`/`questionnaires` 加唯一索引 + `INSERT OR REPLACE`(顺带修 AUD8/双击重复);研究员重置清 token;新增续接回归测试。
- ~~每轮满意度题~~ → 问卷加 1 题(7点,`questionnaires.satisfaction`)。
- ~~AUD8 无唯一约束 + 双击重复~~ → 见 AUD6。

### 2026-06-26 · 第二轮全项目复审(33 智能体 + 对抗验证)
- ~~N1 无超时 + edgefn"渠道繁忙"挂 ~600s~~ → 加 `REQUEST_TIMEOUT=120`(治标,治本=B9 已定 OpenAI)。
- ~~N2 `<think>` 思维链流式直出被试~~ → 新增 `llm.stream_clean()` 边播边擦;单测全过。
- ~~N3 E 引导 fallback 开放题可零内容提交~~ → 改为必须有自填文字。
- ~~N4 E follow-up 时间归入 t_postgen~~ → **复核判非 bug**(与 D 反馈打字时间对称);要改是重定义口径,待定。
- ~~N5(=AUD7)人口学/published 存本地化串~~ → 统一存 index。
- ~~开局问卷增 3 项基线协变量~~ → `aiexp_idx` / `trust` / `own_trait`,均前测;控制需求经权衡未加。

### 2026-06-26 · 首轮全项目审计(10 维 + 对抗验证;62→60 确认)
- ~~AUD1 D 通道失败不回滚~~ → 照搬 E"成功后才提交"守卫。
- ~~AUD2 D 通道空串判定~~ → 改 `out and out.strip()`(与 AUD1 同处)。
- ~~AUD3 E `t_pregen` 把生成等待算进创作时间~~ → 新增 `r_llm_wait_pre` 扣除,只改 E 分支。
- ~~AUD4 语言器对被试可见 + `ai_decided` 串比对脆弱~~ → 治本存语言无关布尔(治标在 JP6 进一步定型)。
- ~~AUD5 E 追问失败被锁死~~ → 加"取消/返回草稿"出口。
- ~~AUD7 published 存本地化串~~ → 见 N5。
- ~~UX 批~~ → 决定键改绿、未答全指明题、等待文案+前置重试、模型连通测试、蓝主色跟随系统。
- ~~引导分页答案丢失 bug~~ → `r_g_answers` 持久化。
- **误报剔除**:devtools 覆盖、intent 无提示、无 UNIQUE 不可复现、widget 键累积、guidance 重试窗、JSON 英文提示。

### 2026-06-13 · v3 引导式共创重构(E2E 全绿)
- ~~C1 prompt 工程 / C3 数据层迁移 / C4 按钮文案~~ → 全部随 v3 落地。
- ~~C5 旧资产清理~~ → ModeMirror/HLZ 标 deprecated(残留死代码清理见 §可延后)。
- ~~C2 mimo 结构化输出测试~~ → 质量 8/8 通过(mimo 已退役;延迟问题归 B9=OpenAI)。

### 更早决策(2026-06-12 ~ 25)
- ~~A2 ghost-run 机器对照~~ → 废弃(D10):C/D 即对照。
- ~~B1 意图基准~~ → 创意 1-2 句即基准(D12)。
- ~~B2 loop 结构~~ → 看稿后[提交/继续引导/手改],无上限(D13/D20)。
- ~~B4 新颖性核查~~ → 完成(paper/9):收窄合取成立。
- ~~B5 D 的角色~~ → 核心对仗:D=先生成再改,E=先引导再生成(D15/D18)。
- ~~Q1-Q6 交互细节~~ → 全部拍板(D17-D22),见 paper/7 v4.1。
- ~~量表点数 7 vs 5~~ → 保留 7 点(源量表原生 7 点,2026-06-25)。
