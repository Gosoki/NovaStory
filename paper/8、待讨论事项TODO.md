# 待讨论事项 TODO

> 建立 2026-06-12;06-26 大清理 + 改写成"人话"(技术项先一句大白话,再附代码细节);**07-02 大整理**(第三轮复审 + 两批拍板 + 日志批次落地后重排)。
> **开放项在上、已结清沉到文末「✅ 已结清(留痕)」。**
> 权威指针:设计=paper/7 v4.1;论文骨架=paper/10;主张=paper/9;运行流程=paper/6;**数据字典=paper/12**;文献=paper/reference/;时间轴=paper/11。

---

## 🧭 速览(2026-07-02 深夜)

**一句话状态:** 应用层已完备——主流程从头到尾、刷新/断网续接、语言锁定、总问卷、token 记录、毫秒级操作日志全部就绪;三轮全项目复审(10 维 → 33 → 66 智能体)把「跑被试前」级代码问题**清零**。卡进度的只剩实验执行层(伦理审查);分析层已拍板全部延后。

**还需要你定的(2026-07-18 案头研究批次后;详见 §🆕 与 paper/14):**
1. **E 是否加第 4 固定维「节奏/尺配分(pacing)」**——研究识别的最贴任务漏项(15 秒怎么切三镜,絵コンテ标准必填)。
2. **三维措辞是否按背书校准**(psychology→欲望/动机、turning_point→价值翻转、key_shot→戏剧核心/构图);背书已确立,校准是优化项。
3. **own3 归属**:改标注为「Pierce 自我投入维度」或换纯 possession 措辞。
4. **是否做预注册(OSF)**——推荐做;无 pilot 下改据文献先验锁主要终点。
5. **质量评价路线**:结构代理 + 不劣性(TOST)为主,是否保留极小样本人评做效度锚。
6. **是否加「AI 问得好不好」问卷题**(你的新需求)——放 E 轮问卷,测被试对引导问题质量的评价;定措辞与位置。
7. **pilot 做不做**(你倾向不做/再看看)——不做则预注册改据文献先验锁终点(paper/14 §4)。

**执行队列(方向已定,只欠动手):** 主题机器基线 norming(冻结题面前)→ A6 分析管线按客观评价栈重写(paper/14 §5)→ 预注册(若做,OSF,先验锁终点)→ 正式采数 → paper/4 重写。

**已拍板决策索引**(编号供后文引用;细节在对应小节,过程见 paper/11):
- **6-25/26**:量表 7 点(6-25)· 正式模型=OpenAI(开发期免费 edgefn)· 招募=日本大学内大学生 · 语言=consent 选一次后锁定
- **7-02**:①不完整数据弃用(未完成被试不进分析)②日志精简批次(**已实装**,→§📋)③**A4 主要终点候选组合+锁定规则**(→⚪A4)④评估无外部真人(只有自评+算法)⑤报酬=无偿志愿者 ⑥judge=全 OpenAI(单家族×3+自一致性)⑦N4 口径维持(AI 用时/人用时天然分离:t_read_intent/t_pregen/t_postgen 为净人时,t_llm_wait 单列,t_total 为毛时长=净人时+等待)⑧分析层全延后、数据先备足

---

## 🆕 案头研究批次(2026-07-18)—— 成果与新待办

> 7 智能体研究工作流 + 引用对抗核查(捏造风险=低)。完整依据与论文取用文本 → **paper/14**;数据合规 → **paper/13**。

**✅ 已应用(代码已改、E2E 绿)**:
- **soa1-2 换 J-SoAS SoPA 情境化**(Xu et al. 2024,[A] 逐字核实):i18n ja/zh/en 三语同步(JP1 结清)。
- **主题重设计**:topics.json 三题全换(拾った切符/最後のひと口/はじめての街の最初の一歩,基调正交),连带 state.py seed + devtools 测试创意同步。⚠️ **冻结前必做机器基线 norming**(paper/14 §3)。
- **数据管理文档**:paper/13(新合规点:被试文本会送 OpenAI,同意书须写明)。

**📌 已确立(可直接写论文)**:E 三维内容效度背书(McKee/Field/Katz/絵コンテ,paper/14 §1,投稿前修 3 处逐字引用);客观评价方案(数据科学栈,paper/14 §5);预注册做法 + 无 pilot 调整(paper/14 §4)。

**🆕 新待办**:
- **B4 提示词正式实验前必须重新校验**(换 OpenAI 后重测引导出题质量/延迟/JSON 健壮性)。
- **主题 norming 脚本**(baseline_gen + 四指标:生成发散度/结局枚举数/三维覆盖率/合规率),冻结题面前跑。
- **A6 分析管线 v3 重写**改为按 paper/14 §5 客观评价栈;A1/judge 从「主评」降为「可选小样本效度锚」。
- **「AI 问得好不好」问卷题**(用户需求):E 轮问卷加 1 题评价引导问题质量;措辞/位置待定。

## 🟡 可延后(不影响被试跑通,数据洁净/边角)

- **AUD9 引导步模型没落库**——**已基本消解**(引导步与主模型同为 OpenAI);真想留审计痕迹可低优先补记。
- **E 引导数量约束只靠 prompt**(7-02 复审):固定3维/5-7问/追问1-3问/每问3-4选项没有实时校验,模型偏离会原样呈现;问答数据已全落库可事后审计。加校验有误杀风险(偏离→误触发 fallback),暂观察 pilot 实测。
- **E 追问 prompt 不带历史问题**——追问轮可能重复问过的问题(概率低,模型能看到当前稿)。
- **LLM 返回空串(无异常)时无错误提示**——C/D 只剩裸重试按钮、E 终稿/D 修改静默无反应(罕见:正常失败都走异常路径)。
- **多标签页同 token 并发无守卫**——两个标签页各自推进同一被试会互相覆盖(单人单坐的实验,现实概率极低)。
- **final_survey 提交→置 done 之间断线**,续接会让被试重填总问卷(秒级窗口)。
- **旧开发库重复行会让续接跳轮**——`count_questionnaires` 用 COUNT(*);正式新库有唯一索引,无此问题。
- **杂项 nit**:注意力检查只存对/错没存原始作答;DB 连接用完没显式关;JSON 模式没用官方 response_format;逐镜头三标签语义略重叠;"15秒・3カット"硬/软约束没向被试讲清;历史面板默认展开占首屏;进度条在 final_survey 阶段不显示(可接受);consent+管理员两个语言器同时渲染显示可能不一致(仅管理员,点一下就好);`reset_for_next` 后研究员面板仍开着(重置后先自己收起再交给被试);`load_topics` 对损坏文件静默回退种子主题(已不会崩,但不告警);C/D 生成中杂散 rerun 会整段重新生成(无幂等守卫,未观测到实际触发);研究员密码兜底 `nova`(**部署时务必在 secrets 配置**)。
- (已结清项不再列此:CG1/CG3/切条件计时/AUD10 进度条/死代码清理等 → 文末「7-02」两条。6-26 审计的原始 40 条 nit 清单文件是临时产物已佚失;7-02 第三轮复审已重新全量清查,残余项即上文所列。)

## 🌏 日语/跨语言效度(pilot/预注册前)

- **JP1 量表日译效度**:✅ **soa1-2 已换 J-SoAS SoPA 情境化(2026-07-18,paper/14 §2.1,[A] 逐字核实)**;own1-3 仍需回译 + 认知访谈,own3 归属待定(paper/14 §2.2);无 pilot → α 事后报,Limitations 写明「未经独立预试」。
- **JP2 日语 embedding 保真效度**:embedding 余弦在日语短文本上可比性存疑。→ **已并入 A4 拍板**:embedding 进保真复合候选、凭 pilot 收敛验收(r≥.3),不达标降次要(见 ⚪A4)。
- **JP3 LLM-judge 日语可靠性**:效度证据多基于英文。~~人评校验用日语母语评审~~ → **人评不可行(拍板④)**;~~异家族×3~~ → **全 OpenAI(拍板⑥)**。代偿:OpenAI 系×3 报自一致性;judge 提示改日语(随 A1 重写);Limitations 明示"单家族+无人评";judge 仅评"保真/结构化 rubric"不评创意(TTCW 边界)。
- **JP4 文化反应偏差**:日本被试爱选中点/谦逊,压缩量表方差。→ 主分析用被试内差分;查条件×反应风格交互;Limitations 写明。
- ~~**JP5 字数门槛**~~ → **用户决定不关注**(`MIN_INTENT_CHARS=10` 保持现状)。
- ~~**JP6 锁语言**~~ → **已实现**(见文末已结清):consent 前选一次 + 之后只有管理员能切。
- ~~**JP7 日本招募**~~ → **已定:日本大学内大学生**(见 B8)。

## 📋 详细操作日志 — ✅ 已实装(2026-07-02 深夜,精简版)

> **现状能算什么/缺什么的完整盘点 → `paper/12、数据字典与可计算指标.md`**。
> **已实装**:LOG3 events.ts 升毫秒 + `seq_in_round` 轮内序号;LOG4 `attempt` 会话段标记(每次轮重开换新 id,重做轮事件不再混段)+ `trial_submit` 后把 `trial_id` 回填到中签 attempt 的全部事件;新增 `guidance_answer_saved` 事件(E 逐题作答时刻,配合毫秒戳可算"每问耗时")。实测:毫秒✓ 序号按 attempt 重排✓ trial_id 只回填中签段✓,E2E 全绿。
> **放弃**:LOG1 每事件存具体内容(弃用不完整数据 → 与 trials 全文重复);LOG2 统一入口重构;LOG5(全版本已存可后验 diff)。~~LOG6 token~~ → 已实装(7-02 白天)。

## 🟢 重写 paper/4 时(文档层)

- **B6 引导维度内容效度**:三维度(心理/转折/画面)需教材/从业者背书(reference/06);防"实验者自由度"。
- **B7 解释矩阵加反向分支**:Knijnenburg 2011 + Slovic 1995 →"新手嫌引导麻烦/满意度与所有权分离"。
- **效率口径三并报**:事前投入 + 事后修改量 + 总投入;轮数口径差异声明(E 一轮≠D 一轮)。
- **A3 同质化叙事位置**:降为探索性。
- 贡献声明用 paper/9 §3;Related Work 按 reference/01 切割;防御段用 reference/05。
- 顶部数据规格按已修代码更新:`r_llm_wait_pre`、`ai_decided` 布尔、人口学存 index、开局新增 `aiexp_idx`/`trust`/`own_trait`、每轮 `satisfaction`、`token` 续接列、`final_survey_json` 总问卷。

## 🔵 pilot 前/中(实验执行层)

- **B3 pilot 监控**:轮数/净用时分布;E 第 1 轮 AI 代答率 >50% 预警(补充维上限 4→2);D 的 AI 通道使用率;引导步实测延迟。
- ~~**B8 被试渠道**~~ → **已定:日本大学内大学生**(大学池招募);~~报酬按大学池估~~ → **无偿志愿者(7-02 拍板⑤)**,可达量 pilot 实测。
- ~~**B9 正式模型**~~ → **已定:正式实验全程 OpenAI**(质量稳/延迟低,30 人 <$5);开发期用免费 edgefn。
- 伦理审查流程确认(研究科);预注册时点 = pilot 后、收数前(OSF)。**← 仍待办**(7-02 用户决定**暂缓启动**;提醒:批准前不能招真人,委员会通常按月开会,倒排 IUI 2027 需 pilot ≤7 月中旬——别让这条吃掉窗口)。

## ⚪ 分析层(已拍板⑧:全部延后、不设期限;A4 口径已定,随时可开工)

- **A6 分析管线 v3 重写(7-02 复审新立项)**——大白话:`analysis/{metrics,stats,power_sim}.py` 还是 v2(ModeMirror/HLZ)时代的,**拿到真数据也算不出论文要的东西**:主 DV 绑定已废弃的 hlz_z(LMM/Wilcoxon/剂量-反应全会跳过)、读恒空的 v2 旧列(edit_dist 会产出假 0 值)、questionnaires 表(主观量表=主要终点)和 v3 努力指标列(t_pregen/t_postgen/n_* → 图3 招牌图)完全没进管线;计划对比缺 E−C;CLMM/负二项/敏感性没实现。**数据采集不受影响**(DB 落库完整),但分析前必须重写;依赖 A4 的主要终点组合,故顺序 = A4 拍板 → 管线重写 → pilot 数据试跑。三个文件与 Makefile 已加过时横幅(7-02)。
- **A1 质量 rubric 终稿**:四维已定(景别/节奏转折/视听语言/情绪锚点);待定每维锚点。~~人评份数(建议 30)~~ → **取消人评(拍板④)**;~~judge 模型待定~~ → **全用 OpenAI(拍板⑥)**:OpenAI 系×3 + 自一致性,具体型号 A1 终稿时定,Limitations 明示单家族+无人评。**注(7-02):`judge.py` 现状与 §6.3 有差距**——缺"盲评保真"模式(保真三角的外部腿)、质量四维与 A1 已定 rubric 不一致、PARSE_FAIL 记录续跑不重试、judge 提示是中文而评审对象是日语脚本(JP3)、docstring/CLI 仍写"异家族"协议(拍板⑥作废)、--human-sample 人评导出已废弃(拍板④)。脚本已加过时横幅(7-02),A1 拍板时一并重写。
- **A4 主要终点 —— 已拍板(7-02,候选组合+决策规则;预注册时执行锁定):**
  - **保真复合(候选)**= 想象匹配 + 意图违背(反向) + 逐镜头 mine 比例 + **创意↔终稿 embedding 相似度**,各成分被试内 z 标准化后取均值(公式进预注册)。**pilot 验收规则**:embedding 与其余三成分方向一致且收敛相关达标(建议 r≥.3)→ 留;不达标 → 降次要,复合缩三件套(JP2 的处置)。
  - **所有权复合**:own1-3 与 soa1-2 **都保留、都报告**;主终点**默认 own1-3 均分**(soa1-2 次要)。若 pilot 显示 own 有测量问题(日语 α 崩 / 天花板),可在**预注册前**改判为 soa 或双主终点——**预注册锁定后、正式数据到手后不可再换**(用户"哪个好用用哪个"的合法实现:决定权在 pilot 数据,不在正式数据)。
  - **盲评保真 = LLM-judge 全自动,固定次要/探索**(用户约束:请不到真人评审):OpenAI 系×3 取均值(拍板⑥,放弃异家族),报自一致性作可靠性代理;"单家族+无人评"进 Limitations,引 Zheng 2023(结构化评分可用)/TTCW(创意维不可靠 → 只评保真不评创意)界定适用范围。
  - **→ A6 管线重写可以照此开工**(embedding 双状态:复合内/次要,两版都算即可)。
- **A5 D 反馈编码 codebook**:`revision_requests` 映射到引导维度(图4);~~两名编码者+一致性~~ → 受拍板④影响改备选:研究员单编码 + LLM 作第二编码者,报人-机一致性,Limitations 说明。
- **A3 执行**:探索性同质化(机器地板=baseline_gen;脚本已修好可跑,`--lang ja`,7-02)。
- ~~**C6 token 统计** = LOG6~~ → **已实装(7-02)**,分析端从 `events.llm_done.payload.usage` 汇总。

---

## ✅ 已结清(留痕)

### 2026-07-02(深夜③)· 问卷前分镜预览表
- 每轮问卷**开头**把终稿渲染成分镜表(No. / 景别·カメラ / 画面 / 情节 / 台词;前 3 镜连续编号 1.2.3 + 第 4 行整行留空),替代原来的纯文本回顾;被试答题前先视觉化确认自己的成品。解析失败(散文稿)时退回纯文本,不出破表。
- 为此**恢复 `shots.py` 的 shot_type 解析**(填 景别/カメラ 列;7-02 白天曾作死代码删除,现有真实消费者);HTML 表格用户文本 `html.escape` 防注入。
- **视觉升级(07-03)**:做成真·絵コンテ纸样式——「画面」列改为 **16:9 空画框**(相框内阴影)+ 圆圈剪辑号 + 秒数角标 + 纸张底色 + 顶部标题栏(絵コンテ + 本轮题目名)。纯 CSS(方法 1;另评估过 PIL/SVG/AI 生图,前者字体慢、后者=未来占位)。占位「今後実装予定」在画框内。
- 新增 i18n `storyboard.*`(col_shot/frame/plot/line + frame_todo)三语 181 键;验证:parser 捕获 shot_type(zh+ja)、HTML 结构/占位×3/转义/编号 1-3、fallback、E2E 全绿。
- **⏳ 未来功能(占位已埋)**:「画面」列现填占位「后续更新 / 今後実装予定」——预留**为每镜生成分镜草图**(文生图),用户日后决定是否开发。

### 2026-07-02(深夜)· LOG 精简批次实装 + 进度条 + 决策批②(报酬/judge/N4/分析延后)
- ~~LOG3/LOG4/E 逐题时刻~~ → **实装**(见 §📋):events 毫秒 ts + `seq_in_round` + `attempt` 段标记 + `trial_id` 回填 + `guidance_answer_saved`;三项 DB 迁移向后兼容,E2E 全绿,机制单测(毫秒/序号/分段/回填)全过。
- ~~AUD10 进度条~~ → **实装**:引导出题步线程化 + st.progress(60s 封顶缓动;`add_script_run_ctx` 保 session_state 线程可用,AppTest 兼容);三语等待文案去掉长等待措辞。
- **决策批②**:报酬=无偿志愿者;judge=OpenAI 单家族×3;N4 维持现状(AI 用时/用户用时天然可分:t_read_intent/t_pregen/t_postgen 为净人时、t_llm_wait 单列、t_total 为毛时长);分析层全延后、数据侧优先(本批次即数据侧收尾)。

### 2026-07-02 · 第三轮全项目复审(66 智能体,9 维查找+逐条对抗验证;104 条确认)+ 修复批次
**数据质量(被试数据直接受益):**
- ~~LOG6 token 用量不记~~ → **实装**:`generate_stream` 开 `stream_options={"include_usage": True}`(edgefn+OpenAI 真机实测兼容)、`generate_json` 读 `resp.usage` 且跨重试累计;经 session_state 传递(并发安全),`llm_done` 事件 payload 带 `usage`,取不到记 null。
- ~~LLM 失败等待灌进创作净时长~~(新发现,med-high):失败/重试的等待(最多 120s×次)原先不计入 `r_llm_wait`,会虚增核心 DV t_pregen/t_postgen → 失败路径也调 `add_llm_wait`。
- ~~被试刷新续接后该轮 t_total/t_read_intent 落 NULL~~(新发现,high;与已知"切条件"同根):`_attempt_resume` 清空 `r_events` 后没补时间锚 → 补记 `round_start`;研究员切条件按钮同修(原 🟡 项一并结清)。
- ~~CG1 抢序号~~ → `BEGIN IMMEDIATE` 原子化(9 线程并发实测拿到 0-8 不撞);顺带:**研究员注入的 dev 测试被试不再占用拉丁方计数**(json_extract 过滤,收数期间测试不打乱条件平衡,实测通过)。
- ~~CG3 脏主题崩溃~~ → `_int_or` 兜底(state+prompts 双层)+ 非 dict 条目过滤。
- ~~shots 解析器两个新 bug~~(6-27 新逻辑的边界,对抗验证实测复现):① 杂散时长标签(如"合計 15 秒"行)会以"块数多者胜"推翻本来正确的编号切分 → dur 切分加双守卫(首标记前不得有字段标签;无 visual 的块并入前块);② 时长字段在句尾时错位切分仍报 parse_ok=1(静默污染逐镜头数据)→ 同守卫拒绝、诚实 parse_ok=0;另修:dur 切分残尾编号清理、`【時間】`变体识别。11 个回归+对抗用例全过,6-27 场景无回退。
**被试体验:**
- ~~E 追问引导"问题生成失败"分支锁死~~(新发现,high;AUD5 同类未盖分支):失败重试页没有渲染"返回草稿"出口,网关持续繁忙时被试被困 → 失败分支补同款出口。
- ~~consent 语言器暴露 en~~:选 en 会得到"英文 UI+日文 AI 输出"混语言会话(prompts 只支持 ja/zh)→ 被试端只开 ja/zh,en 仅管理员。
- ~~devtools 一键填充漏满意度题~~(6-26 新题没跟上)→ 补 `_q_sat_`。
**死代码清理(用户要求"精简完全没用上的"):**
- 删除:`scripts/ghost_run.py`(已废弃+调不存在函数,连带 Makefile ghosts 目标)、~~`shots.py` shot_type 字段~~(**7-02 深夜③已恢复**——问卷前的分镜表 景别/カメラ 列要用,见文末「分镜预览表」)、`state.is_last_round`/`save_topic_preset`、`config.CONDITIONS`/`GUIDANCE_FIXED_DIMS`(零引用)、i18n 死键 `errors.outline_empty`×3、`trials` 表 7 个 v2 旧列出 schema(旧库既有列保留无害)、空目录 `samples/vsc_workspace`、`prompts._answer_lines` 死参。
- 修复保留:`baseline_gen.py`(A3 还要用)——调不存在的 `build_user`→`build_user_script`、scenario `{ja,zh}` 字典本地化、加 `--lang`(默认 ja);`llm_batch` 加 120s 超时。
- 标记不删:`analysis/{metrics,stats,power_sim}.py` 加"v2 时代,收数前必须重写"横幅 → 立项 A6。
**其他:** prompts ja 版补「单镜时长自行分配」句(zh/ja 对称);config 引导步注释按 B9 拍板更新。
**文档同步:** paper/6 整体重写(语言流/续接/总问卷/满意度/topics/模型全对齐现状);README 4 处(token/final_survey/satisfaction/ja fallback/ghost 删除);paper/10 回写招募与满意度拍板、§6.2 补 satisfaction/final_survey 行、删 6/30 投稿项;paper/4 横幅补招募拍板与 ghost-run 删除;paper/5 补实现口径注;paper/7 横幅 OpenAI 拍板措辞;reference/README v4.1。
**验证:** E2E 全绿(含续接回归)+ shots 11 用例 + CG1 并发实测 + token 真机(流式)实测。

### 2026-06-27 · 逐镜头解析健壮性(parse_ok)+ prompt 编号
- ~~终稿没编号(`1. 2. 3.`)就切不开镜头 → 问卷掉"整体"兜底、`parse_ok=0`、丢逐镜头保真那条腿~~ → **两端都修**:
  - **解析器**(`core/shots.py`):没编号/部分编号时,**改用「时长/秒数字段」当镜头分界**(每镜都以时长起头);实测 用户那段无编号稿、ja 无编号、部分编号 全部正确切成 3 镜,纯散文/单镜仍正确兜底。
  - **prompt**(`core/prompts.py` 4 个 system 提示,zh+ja,生成+修订):把"编号列表"强化成**「必须以 `1. 2. 3.` 开头」+ 给格式示例**;修订提示额外注明**「原稿没编号也要补上」**(解决多次提交时编号丢失的传播)。
- **JSON 化剧本的取舍(讨论结论)**:**不做**。剧本是被试要"看着流式生成 + 直接手改"的文档,改 JSON 会废掉流式直出和文本编辑、且 JSON 转义对长台词更易翻车(引导步 JSON 已有重试/降级为证)。结论=对人用 markdown、解析器兜底;真要保证逐镜头可后期加"提交后结构化抽取"旁路(暂不做)。

### 2026-06-26(深夜)· final_survey 总问卷 + JP6 语言流 + 决策落定(=paper/11「深夜」条)
- ~~未来"总问卷"~~ → **已实现**:三轮做完 → 新 `final_survey` 阶段(最喜欢哪轮 / 以后用哪轮 / 总体满意度 7点 / 自由留言),存 `participants.final_survey_json`;选项用「轮次号+题目标题」作记忆锚,分析端按 `trials` 把轮次映射到 C/D/E。新增阶段进 state 机 + 路由 + 续接(刷新回 final_survey)+ E2E。
- ~~JP6 锁语言~~ → **已实现**:语言器移到 **consent 页**(被试开头选一次)+ 移进**管理员区**(管理员随时可切);被试进入后不再看到语言器。技术上换成不占 `lang` 控件键 + `on_change`(新 `views/_lang.py`),避开"控件卸载清空 lang"和"format_func 崩测试"两个坑。
- ~~B9 正式模型~~ → **拍板:正式实验全程 OpenAI**;开发期继续用免费 edgefn(KAT)。
- ~~B8 / JP7 招募~~ → **拍板:日本大学内大学生为对象**。
- ~~AUD9 引导步模型不落库~~ → **决策消解**(引导步与主模型同为 OpenAI,不会对不上;降为可延后审计项)。
- ~~JP5 日语字数门槛~~ → **用户决定不关注**(保持 `MIN_INTENT_CHARS=10`)。

### 2026-06-26(晚)· AUD6 会话恢复 + 满意度题(=paper/11「晚」条)
- ~~AUD6 刷新/断网即丢本轮 + 重复占 seq~~ → **方案 A**:随机 token 进网址 `?t=`,刷新后从 DB 重建身份/序号、跳回第一个没答完问卷的轮次;`trials`/`questionnaires` 加唯一索引 + `INSERT OR REPLACE`(顺带修 AUD8/双击重复);研究员重置清 token;新增续接回归测试。
- ~~每轮满意度题~~ → 问卷加 1 题(7点,`questionnaires.satisfaction`)。
- ~~AUD8 无唯一约束 + 双击重复~~ → 见 AUD6。

### 2026-06-26 · 第二轮全项目复审(33 智能体 + 对抗验证)
- ~~N1 无超时 + edgefn"渠道繁忙"挂 ~600s~~ → 加 `REQUEST_TIMEOUT=120`(治标,治本=B9 已定 OpenAI)。
- ~~N2 `<think>` 思维链流式直出被试~~ → 新增 `llm.stream_clean()` 边播边擦;单测全过。
- ~~N3 E 引导 fallback 开放题可零内容提交~~ → 改为必须有自填文字。
- ~~N4 E follow-up 时间归入 t_postgen~~ → **复核判非 bug**(与 D 反馈打字时间对称);要改是重定义口径 → **7-02 拍板⑦:维持现状**。
- ~~N5(=AUD7)人口学/published 存本地化串~~ → 统一存 index。
- ~~开局问卷增 3 项基线协变量~~ → `aiexp_idx` / `trust` / `own_trait`,均前测;控制需求经权衡未加。

### 2026-06-26 · 首轮全项目审计(10 维 + 对抗验证;62→60 确认)
- ~~AUD1 D 通道失败不回滚~~ → 照搬 E"成功后才提交"守卫。
- ~~AUD2 D 通道空串判定~~ → 改 `out and out.strip()`(与 AUD1 同处)。
- ~~AUD3 E `t_pregen` 把生成等待算进创作时间~~ → 新增 `r_llm_wait_pre` 扣除,只改 E 分支。
- ~~AUD4 语言器对被试可见 + `ai_decided` 串比对脆弱~~ → 治本存语言无关布尔(治标在 JP6 进一步定型)。
- ~~AUD5 E 追问失败被锁死~~ → 加"取消/返回草稿"出口。
- ~~AUD7 published 存本地化串~~ → 见 N5。
- ~~UX 批~~ → 决定键改绿、未答全指明题、等待文案+前置重试、模型连通测试、蓝主色跟随系统。
- ~~引导分页答案丢失 bug~~ → `r_g_answers` 持久化。
- **误报剔除**:devtools 覆盖、intent 无提示、无 UNIQUE 不可复现、widget 键累积、guidance 重试窗、JSON 英文提示。

### 2026-06-13 · v3 引导式共创重构(E2E 全绿)
- ~~C1 prompt 工程 / C3 数据层迁移 / C4 按钮文案~~ → 全部随 v3 落地。
- ~~C5 旧资产清理~~ → ModeMirror/HLZ 标 deprecated(残留死代码清理见 §可延后)。
- ~~C2 mimo 结构化输出测试~~ → 质量 8/8 通过(mimo 已退役;延迟问题归 B9=OpenAI)。

### 更早决策(2026-06-12 ~ 25)
- ~~A2 ghost-run 机器对照~~ → 废弃(D10):C/D 即对照。
- ~~B1 意图基准~~ → 创意 1-2 句即基准(D12)。
- ~~B2 loop 结构~~ → 看稿后[提交/继续引导/手改],无上限(D13/D20)。
- ~~B4 新颖性核查~~ → 完成(paper/9):收窄合取成立。
- ~~B5 D 的角色~~ → 核心对仗:D=先生成再改,E=先引导再生成(D15/D18)。
- ~~Q1-Q6 交互细节~~ → 全部拍板(D17-D22),见 paper/7 v4.1。
- ~~量表点数 7 vs 5~~ → 保留 7 点(源量表原生 7 点,2026-06-25)。
