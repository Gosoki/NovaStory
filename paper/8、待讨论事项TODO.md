# 待讨论事项 TODO

> 建立 2026-06-12;2026-06-26 大清理 + 改写成"人话"(技术项都先一句大白话,再附代码细节)。
> **开放项在上、已结清沉到文末「✅ 已结清(留痕)」。**
> 权威设计=paper/7 v4.1;论文骨架=paper/10;主张=paper/9;文献=paper/reference/。

---

## 🧭 速览:现在该定什么、还差什么(2026-06-26)

**一句话状态:** 系统主流程能从头跑到尾,**刷新/断网也不再丢数据了**(AUD6 已修)。离正式 pilot,**就差"正式实验用哪个模型"这一个决定**。

**需要你拍板的(就这几件):**
1. **正式实验用哪个模型?**——免费的 edgefn 偶尔会卡(已加超时兜底,但不稳);还是花钱但稳定快的 OpenAI(30 人全程 <$5)。→ 见 B9
2. **从哪里招日本被试?**——平台/渠道还没定,影响报酬和能招到多少人。→ 见 B8
3. **"花了多少 token(=多少钱)"现在就开始记吗?**——现在完全没记;被试一旦做完,这批账永远查不回来了。改动很小。→ 见"补不回来"
4. **论文主分析以哪几个指标为准?**——预注册前必须钉死,否则有"挑数据"嫌疑。→ 见 A4

**可以慢慢来的:** 一堆数据洁净/边角小问题,不影响被试跑通,见 §🟡 可延后。

**未来(你提过):** 三轮全部做完后,再加一份"总问卷"(三条件偏好排序 + 总体满意度 + 下次会用哪种)。先占位,等你说要再做。

---

## 🔴 跑被试前值得处理

### 要你拍板
- **B9 正式实验模型**(同"速览 1"):免费 edgefn(KAT,偶尔卡) vs 全程 OpenAI(稳)。pilot 实测延迟后定。已加超时兜底是"少卡一会儿",真要稳还得换。**注:mimo 已退役。**
- **token 要不要现在就记**(同"速览 3",属下面"补不回来"里最便宜的一项,~10 行)。
- **未来的"总问卷"**:你说"全部做完后再问几题"。位置在三轮结束后、完成码之前加一页。需要时我做。

### 补不回来(被试做完即永久丢失,跑之前最好先埋好)
- **token 用量**——大白话:每次叫 AI 干活都花钱(按字数算),现在**一个数都没记**。被试做完,这批"花了多少"就再也查不到了。
  - 细节:`generate_json` 其实已经拿到了用量却扔了;`generate_stream` 没开统计开关。→ 读出来记进 `llm_done` 事件(`{prompt,completion,total_tokens}`)。注:个别免费网关流式不返用量,取不到记空别崩。
- **E 提问用的是哪个模型没记**——大白话:E 的"AI 提问"那步**可以**配一个和主模型不同的模型;如果真这么配了,现在不记是哪个,以后没法复现/查证。
  - 细节:`GUIDANCE_API_INDEX` 指独立模型时,trial 只存了主模型。→ 把提问那步实际用的模型也记进 `guidance_shown` 事件(AUD9)。
- **操作日志记得太粗**——大白话:① 时间只精确到"秒",同一秒点了好几下就分不清先后;② 没存被试**具体选了哪个、写了什么字**(只存了"写了几个字");被试中途退出,这些就丢了。
  - 细节:`events.ts` 升毫秒;`guidance_answer` 存所选文本、`revision_request` 存原文;加 `trial_id` 关联(LOG1-4)。

## 🟡 可延后(不影响被试跑通,数据洁净/边角)

- **CG1 抢序号**——大白话:两个被试**几乎同一瞬间**通过筛查,可能抢到同一个"拉丁方序号",让条件顺序略微不平衡。被试少、几乎不会撞,可暂留。
  - 细节:`COUNT(*)%9` 与 INSERT 非原子。→ `BEGIN IMMEDIATE` 或按自增 id 推导 seq。
- **CG3 脏主题数据会崩**——大白话:`topics.json` 里如果混进格式不对的旧数据,加载时会直接报错。→ `int()` 加个兜底。
- **研究员"切条件"按钮会把用时记成空**——大白话:**只有研究员测试时**用那个切换按钮,会让那条记录的用时变空白(被试正常流程不受影响)。→ 切换后补记一次开始时间。
- **AUD10 等待只有转圈、没进度条**——大白话:AI 生成要等几十秒,现在只有转圈 + 一句文案,没有进度条/倒计时,长等待容易让人想刷新。→ 加动态进度(关联 B9 延迟)。
- **AUD-low/nit(约 40 条,择机随手)**:注意力检查只存了对/错没存原始作答;数据库连接用完没显式关;JSON 模式没用官方"只输出 JSON"开关;逐镜头三个标签(我的/AI但我认可/违背我意)语义有点重叠;"15秒・3カット"是硬要求还是建议没向被试讲清;历史面板默认展开占首屏。**死代码**(不在被试路径,纯研究员离线脚本):`scripts/ghost_run.py` 调了不存在的函数会报错、`baseline_gen.py`/`ghost_run.py` 调函数没传语言、`shots.py` 的 `shot_type` 字段没人用、`trials` 表一堆 v2 旧版遗留列永远是空。**完整清单见审计原始结果**(`tasks/wxqj7vwwt.output`)。

## 🌏 日语/跨语言效度(pilot/预注册前)

- **JP1 量表日译效度**:所有权(英文母本)和 agency(只取 2 题且自译)的日译删减版效度未知。→ 用 J-SoAS 已发表日语原句;所有权做回译+认知访谈;pilot 报日语 Cronbach α;论文区分"已验证量表"vs"自译版"。
- **JP2 日语 embedding 保真效度**:embedding 余弦在日语短文本上可比性存疑。→ pilot 验收敛效度;主锚用盲评+逐镜头,embedding 降辅助。
- **JP3 LLM-judge 日语可靠性**:效度证据多基于英文。→ 人评校验用**日语母语评审**,单独报日语 judge-人评相关。
- **JP4 文化反应偏差**:日本被试爱选中点/谦逊,压缩量表方差。→ 主分析用被试内差分;查条件×反应风格交互;Limitations 写明。
- **JP5 字数门槛**:`MIN_INTENT_CHARS=10` 在日语(假名助词多)信息量更低。→ 改语言感知门槛或用形态素/词数(MeCab/SudachiPy)。
- **JP6 锁语言**:语言选择器被试可中途切(数据污染面已被布尔修复堵住)。→ 正式收数 consent 后锁定;每轮 lang 写 events 校验三轮一致。
- **JP7 日本招募**:Prolific 日本池小 + novice 准入更窄。→ pilot 实测可达量;备 CrowdWorks/Lancers/大学池(同 B8)。

## 📋 详细操作日志 — 整套留到系统定型后一次性加(现在不做)

> 目标:每个被试每个操作可逐条复盘——**时间 + 动作 + 具体选了/写了什么 + 脚本改没改 + token**。地基(`events` 表 + `log_event`)已在。
> LOG1 每个事件存"具体内容"(选项原值+输入全文);LOG2 收口到一个 `log_action` 统一入口;LOG3 时间升毫秒/加轮内序号;LOG4 加 `trial_id` 关联;LOG5 显式标"脚本改没改"(全版本已存,可后验 diff);**LOG6 token(=上面"补不回来"那条,唯一建议提前)**。
> 时机:系统冻结后一次迁移完成。

## 🟢 重写 paper/4 时(文档层)

- **B6 引导维度内容效度**:三维度(心理/转折/画面)需教材/从业者背书(reference/06);防"实验者自由度"。
- **B7 解释矩阵加反向分支**:Knijnenburg 2011 + Slovic 1995 →"新手嫌引导麻烦/满意度与所有权分离"分支。
- **效率口径三并报**:事前投入 + 事后修改量 + 总投入;轮数口径差异声明(E 一轮≠D 一轮)。
- **A3 同质化叙事位置**:降为探索性。
- 贡献声明用 paper/9 §3;Related Work 按 reference/01 切割;防御段用 reference/05。
- 顶部数据规格按已修代码更新:`r_llm_wait_pre`、`ai_decided` 布尔、人口学存 index、开局新增 `aiexp_idx`/`trust`/`own_trait`、问卷新增 `satisfaction`、`token` 续接列。

## 🔵 pilot 前/中(实验执行层)

- **B3 pilot 监控**:轮数/净用时分布;E 第 1 轮 AI 代答率 >50% 预警(补充维上限 4→2);D 的 AI 通道使用率;引导步实测延迟(>30s 切 OpenAI)。
- **B8 被试语言/渠道(一直未定,同"速览 2"):pilot 前必须定。** 大方向已锁日语,具体平台未定。
- **B9 正式模型锁定(同"速览 1")**:pilot 看延迟后定。
- 伦理审查流程确认(研究科);预注册时点 = pilot 后、收数前(OSF)。

## ⚪ 分析层(拿到数据后)

- **A1 质量 rubric 终稿**:四维已定(景别/节奏转折/视听语言/情绪锚点);待定每维锚点、judge 模型、人评份数(建议 30)。
- **A4 主要终点确切组合(同"速览 4"):** 预注册前钉死"保真复合"(想象匹配+违背反向+逐镜头 mine 比例+盲评)与"所有权复合"(own1-3 均分)。
- **A5 D 反馈编码 codebook**:`revision_requests` 映射到引导维度(图4);两名编码者+一致性。
- **A3 执行**:探索性同质化(机器地板=baseline_gen)。
- **C6 token 统计** = LOG6(建议提前,见上)。

---

## ✅ 已结清(留痕)

### 2026-06-26(下午第三批)· AUD6 会话恢复 + 满意度题
- ~~**AUD6 刷新/断网即丢本轮 + 重复占 seq 破坏拉丁方**~~ → **方案 A 已实现**:`insert_participant` 发一个随机 token、写进网址 `?t=`;刷新后启动若发现该 token 且未完成,就从数据库重建身份/序号、跳回"第一个没答完问卷的那轮"。**配套防重复**:`trials`/`questionnaires` 加唯一索引 + `INSERT OR REPLACE`,重做一轮会覆盖而非新增(顺带把 AUD8/双击重复也解决了)。研究员"重置"会清掉 token。**新增 AppTest 续接回归测试**(刷新→回到第 2 轮、不多插行/不多占 seq),E2E 通过。
- ~~**满意度题**~~ → 每轮问卷加 1 题"这一轮成品我整体满意"(7点,`q.satisfaction`),存 `questionnaires.satisfaction`;i18n 3 语;smoke 同步。(全程"总问卷"另议,见上"未来"。)
- ~~AUD8 无唯一约束 + 双击重复落库~~ → 见 AUD6(唯一索引 + OR REPLACE,一并修)。

### 2026-06-26 · 第二轮全项目复审(33 智能体 + 对抗验证)
- ~~N1 无超时 + edgefn"渠道繁忙"挂 ~600s~~ → 加 `REQUEST_TIMEOUT=120`(治标,治本在 B9)。
- ~~N2 `<think>` 思维链流式直出被试~~ → 新增 `llm.stream_clean()` 边播边擦;单测全过。
- ~~N3 E 引导 fallback 开放题可零内容提交~~ → 改为必须有自填文字。
- ~~N4 E follow-up 时间归入 t_postgen~~ → **复核判非 bug**(与 D 反馈打字时间对称),保持现状;要改是重定义口径,待定。
- ~~N5(=AUD7)人口学/published 存本地化串~~ → 统一存 index。
- ~~开局问卷增 3 项基线协变量~~ → `aiexp_idx`(AI 创作经验)/`trust`(AI 信任)/`own_trait`(所有权倾向),均前测;控制需求经权衡未加。

### 2026-06-26 · 首轮全项目审计(10 维 + 对抗验证;62→60 确认)
- ~~AUD1 D 通道失败不回滚(孤儿请求错配履历)~~ → 照搬 E"成功后才提交"守卫。
- ~~AUD2 D 通道空串判定~~ → 改 `out and out.strip()`,空返回不写版本(与 AUD1 同处)。
- ~~AUD3 E `t_pregen` 把生成等待算进创作时间~~ → 新增 `r_llm_wait_pre` 扣除,只改 E 分支。
- ~~AUD4 语言器对被试可见 + `ai_decided` 串比对脆弱~~ → 治本存语言无关布尔;治标回退为被试可自选。
- ~~AUD5 E 追问失败被锁死~~ → 加"取消/返回草稿"出口。
- ~~AUD7 published 存本地化串~~ → 见 N5。
- ~~UX 批~~ → 决定键改绿、未答全指明题、等待文案+前置重试、模型连通测试、蓝主色跟随系统、语言器外放。
- ~~引导分页答案丢失 bug~~ → `r_g_answers` 持久化。
- **误报剔除**:devtools 覆盖、intent 无提示、无 UNIQUE 不可复现、widget 键累积、guidance 重试窗、JSON 英文提示。

### 2026-06-13 · v3 引导式共创重构(E2E 全绿)
- ~~C1 prompt 工程 / C3 数据层迁移 / C4 按钮文案~~ → 全部随 v3 落地。
- ~~C5 旧资产清理~~ → ModeMirror/HLZ 标 deprecated(残留死代码清理见上 §可延后)。
- ~~C2 mimo 结构化输出测试~~ → 质量 8/8 通过(mimo 已退役 2026-06-26;延迟问题归 B9)。

### 更早决策(2026-06-12 ~ 25)
- ~~A2 ghost-run 机器对照~~ → 废弃(D10):C/D 即对照。
- ~~B1 意图基准~~ → 创意 1-2 句即基准(D12)。
- ~~B2 loop 结构~~ → 看稿后[提交/继续引导/手改],无上限(D13/D20)。
- ~~B4 新颖性核查~~ → 完成(paper/9):收窄合取成立。
- ~~B5 D 的角色~~ → 核心对仗:D=先生成再改,E=先引导再生成(D15/D18)。
- ~~Q1-Q6 交互细节~~ → 全部拍板(D17-D22),见 paper/7 v4.1。
- ~~量表点数 7 vs 5~~ → 保留 7 点(源量表原生 7 点,2026-06-25)。
